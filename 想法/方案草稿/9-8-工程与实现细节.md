# PocketXMol：工程与实现细节

更新：2026-09-11。本文落实[科学契约](9-8-科学契约.md)，规定数据接口、训练和服务器执行方式，并保留有用的资产审计证据；[边界与核查清单](9-8-边界与核查清单.md)用于逐项验收。

本阶段已获准端到端完成必要数据准备、六个无密度模型的训练及原val/loss验证、六模型和官方对照的完整测试与报告。训练后不再安排完整验证集采样或评价。密度几何和候选构造保留为后续规格，正式密度训练仍等待用户查看无密度结果并选定 T／核酸构造及预算。本文描述目标行为，不声称新代码、数据和运行已经通过；实际命令、产物与状态记录在[总日志](../../日志/总日志.md)及其链接的逐实验日志。

## 1. 训练配置与执行语义

首轮为 RA/RB × 中心 T0／中心 T1／包络 T0 六个无密度模型，加冻结官方 O。四个中心模型各评 C0/C5，两个包络模型各评 E，官方评 C0/C5/E，共 13 个模型×协议、39 个测试视图汇总。每实例每模型每协议使用 50 候选、100 步；训练中的原 `val/loss` 与完整采样评价分开。

### 1.1 固定数值

| 参数 | 当前值或行为 |
|---|---|
| 优化器 | 沿用原 AdamW；初始 lr=1e-4，不重写优化器 |
| 其他 AdamW 参数 | weight_decay=0.001、betas=(0.99,0.999)、eps=1e-8；不建旧／新参数学习率分组 |
| warmup | 0 |
| 名义全局批量 | global_batch_size=72；默认单卡，等于单卡 batch×梯度累积次数 |
| 精度 | bf16-mixed |
| 无密度 batch／累积 | 优先单卡72×1、不梯度累积；显存不足可按 36×2、24×3、18×4、12×6 调整 |
| 密度 batch／累积 | 从 24×3 尝试，可降至 18×4、12×6 |
| 偶发 OOM | 允许原 reduce_batch 裁掉少量实例后继续，记录发生情况；不增加补样本或 microbatch 归约等价机制 |
| 验证损失 | 每 800 个优化器更新计算一次原路径 `val/loss` |
| Plateau | mode=min、threshold_mode=rel、threshold=0.01、patience=5、cooldown=0 |
| 下降倍率与下限 | factor=0.2、min_lr=0，沿用 scheduler 默认 eps=1e-8 |
| 停止 | 第三次实际下降后、下一次参数更新前停止；同时最多 40000 个优化器更新，先到者生效 |
| 训练随机种子 | 沿用 train.seed=2023，不安排种子或学习率搜索 |
| 原损失与置信度 | 保留全部原计算和真正反传总键 `mixed/total` 内的 confidence |

连续第 6 次验证没有达到 `current < scheduler_best×(1−0.01)` 时才下降；不是只与上一次验证相比。学习率序列为 `1e-4 → 2e-5 → 4e-6 → 8e-7`，第三个箭头触发停止，不在 8e-7 再更新参数。

### 1.2 模型初始化、验证与恢复

新训练只加载官方 pxm 的 `model.*` 参数，重建优化器和训练包装状态，训练全部原主干及新增受体参数。作者 checkpoint 的 optimizer、scheduler、bad_epoch、best、global_step 等训练状态不加载；scheduler 初始 best 来自本次训练第一次验证。RB 的核酸编码器在官方蛋白参数加载后复制兼容权重，此后独立训练；恢复自己的 checkpoint 时不重复覆盖。

`utils/train.py::get_scheduler` 需补 threshold 等配置接线。原 Lightning 的 `val_check_interval` 计训练 batch，不能把 800 个 microbatch 当作 800 次优化器更新；累积为 2／3 时对应 1600／2400 个 microbatch。每个验证结果只能驱动一次 plateau，不同时由自动配置和回调重复 step。

第三次下降时完成该次调度、记录停止原因并保存状态，在下一次 optimizer step 前结束。40000 恰为 800 的整数倍，到上限时完成对应验证和保存，不执行第 40001 次更新；原 180000／240000 上限不再控制新实验。无限训练 Dataset 与 `max_epochs` 的接线不得提前结束本阶段。

评价使用原 `val/loss` 数值最低的 best；显著改善阈值不改变 best 的数值最低定义。last 用于恢复自己的模型、优化器、scheduler best／坏验证计数、累计下降次数、global_step、主进程 RNG 与已停止标志，并保留实际数据配置和冻结清单路径。多 worker 续训继续按相同 occurrence 均匀有放回分布抽样，不保存预取队列，也不要求中断前后逐样本完全同序。满足正式停止条件的 checkpoint 不通过普通 resume 清零后继续；Slurm 时间结束、手动中断或 OOM 中断均不算训练完成。

六个训练模型的最终 best 与官方冻结 pxm 权重分别运行一次对应 C0/C5/E 的完整测试，不再安排完整验证集采样及评价。每800更新的训练验证保留原 `val/loss`、调度和best选择，不生成50条候选。程序正确性验收仅用非测试数据；不能看测试结果后改训练配置或筛选。

### 1.3 OOM、验证覆盖与检查点

无密度 batch 优先尝试72且不累积，必要时依次改为36、24、18、12及相应累积次数；密度从24开始。偶发原 `reduce_batch` 少量裁批可以继续，日志据实记录；若反复 OOM，则改用较小单卡 batch 和匹配累积，名义 global batch 仍为 72。不建设丢样本补偿、严格归约等价或频繁中断恢复机制。

原 `utils/dataset.py` 的有限验证迭代器采用 `total_size // total_samplers` 分配数量，可能漏掉尾部。新入口修正正常遍历下的 worker 划分，避免漏样和重复，保留原 loss；偶发 OOM 裁批另据实记录，不把正常覆盖缺陷解释成已允许的 OOM。

原 ModelCheckpoint 使用 `save_top_k=-1`。继续保留定期 checkpoint 并标明 best／last，不擅自改为自动淘汰旧文件；空间占用写入日志，需要清理时另获明确许可。不同模型的配置、目录和 checkpoint 互不覆盖。

## 2. 数据接口与历史资产审计

### 2.1 累计筛选、身份与原划分

源资产根 `/storage/penghongen/AdaLigand/Ori_Data` 保持只读。`occurrences.jsonl` 的 `candidate_id` 为 PDB 内稳定整数编号，直接作为本项目 `occurrence_id`；`object_key` 为跨实例共享模板身份。例如 `pdb_id="9lfa"`、`candidate_id=0`、`object_key="CCD:A1EJF"`、`kind="CCD"`、`is_covalent=false`，components 只有一个残基。

所有 split 累计要求：原 `type_tag=small_molecule`、单残基单 CCD、非共价、完整模板含碳、原配体元素／键词表支持、模板图有效、沉积重原子无缺失、语言输入与输出资产完整有效，以及必要受体／地图等资产有效。训练、验证、校准继续继承 `stage1_preparation_box_pool_3/split/` 的实例质量条件；不能只读 PDB 名单后重新纳回其余实例。校准不并入训练。

训练对全部合法实例均匀有放回抽样；身份分组按完整 `object_key`，不跨 CCD 合并。测试 ALL/CAP10/HF10_TO5 共用每身份一次固定排序，只对 ALL 频数大于 10 的身份截取前 10／前 5；原频数 6–10 全保留。

### 2.2 测试源与历史候选审计

指定测试源为 `/storage/penghongen/AdaLigand/held_out/split/held_out_06_chain/test_0.json`。准备阶段读取的文件含 179 个 PDB，`occurrence_filter=null`、`seed=3407`、`pdb_coverage_mode="chain"`、`pdb_coverage_threshold=0.6`；最后一个字段为链覆盖判据，不是 60% 序列一致性。

以下为加入完整性等新条件之前的历史只读审计，保留以说明来源，不能当作当前分母：

| 历史累计检查范围 | 实例数 | PDB 数 | CCD 身份数 |
|---|---:|---:|---:|
| 原 `type_tag=small_molecule` | 821 | 94 | 未单独汇总 |
| 再要求单残基 CCD、非共价 | 814 | 92 | 96 |
| 再检查原词表、键类型及模板／坐标对应 | 671 | 89 | 88 |
| 再要求完整模板含碳 | 583 | 83 | 84 |

词表排除 143 个含 Mg、Fe、As 的实例。671 个中含 `CCD:PO4` 70、`CCD:CL` 9、`CCD:SO4` 8、`CCD:H2S` 1，完整模板均不含碳。含碳的 583 个中，实际 `present` 显示 134 个存在缺失；加入当前完整性条件后须剔除，不再进行缺失子图适配。

历史含碳范围的 ALL/CAP10/HF10_TO5 为 583/352/282；仅扣除三个 FO4 例外后曾推算 580/349/279。这两组数字均未应用全部当前条件，实际三视图重新生成。

583 个完整模板重原子数最小／中位／最大为 3／36／100，观测数为 3／31／79。历史完整模板均可按保存原子、电荷、键构建并 sanitize，未发现多个连通分量；此结论不代替当前完整图手性、对称与模型张量验收。

历史 `exp.npy/exp.npz/sim.npy/sim.npz` 均存在，所查观测原子在 exp 源边界内；这不等于新的 48³ 裁块已通过空间验收。SMI-TED 当时有 580 个通过身份、768 维形状和有限值核对，缺的恰为 `9oq7` 的 FO4 `candidate_id=4,10,13`。它们的旧 SMILES 含异常 `[O-][H]`，记录为 model_failed，完整重原子图本身可以构建；当前直接排除，不修复或补表征，不能因此排除整个 9oq7。

### 2.3 完整重原子接口

`parse/{pdb_id}/ligand_coords.npz` 的 `coords_{cid}` 为 float32 `(M,3)`，`present_{cid}` 为 bool `(M,)`，均与完整模板重原子顺序对齐；缺失位置为 NaN，坐标是世界 XYZ，单位 Å。`centroid_atom_{cid}` 为沉积原子几何中心；通过完整性筛选后它就是完整配体重原子中心。

只接收 `present` 全真、坐标完整有效的实例。历史例子 `("9nvn",0)` 的 `CCD:LBN` 只有 15/52 个沉积重原子，当前直接过滤；不再准备 15 原子训练图和 52 原子推理图之间的映射。训练、推理、评价使用同一完整模板编号，基础图和沉积坐标直接读源文件，不另存副本。

模板键类别需显式转换：AdaLigand 五列为单、双、三、配位、芳香，PocketXMol 类别 1、2、3、4 中 4 是芳香。源芳香第 4 列映射到类别 4，不能用统一 `argmax+1`，配位键不能误认作芳香。

### 2.4 三类获准预计算产物

新根目录为 `/storage/penghongen/Adaligand_Build/Ori_Data`，大小写保持此写法。仅保存以下三类正式数据，产物附近 README 记录实际目录、字段、形状、单位和具体例子：

| 产物 | 组织单位与内容 |
|---|---|
| 必要模板图派生信息 | 按 `object_key` 共享的专用子目录，只补模型确实读取且源模板未提供的字段和必要索引 |
| 对称排列 | 按 `object_key` 共享，可与上一类同文件；保留原训练 `matches_iso` 语义，不重复保存基础图或坐标 |
| 伪 ligand-area 标签 | 专用子目录下按 `pdb_id/occurrence_id` 保存单实例文件，如 `ligand_area/9lfa/0.npy`；直接提取源 `mask_0` 的 int32 `(K,3)` 全源 ZYX 体素索引 |

`K` 为该实例标签覆盖的源体素数。运行时将这些索引与当前实际 48³ 裁块相交并生成块内目标，不重新定义球标签，也不每次打开包含多个实例及大掩码的源 `ligand_area.npz`。标签与 exp／sim 共用源几何；例如源索引 `[25,30,40]` 在起点 `[20,20,20]` 的裁块内为 `[5,10,20]`，此例仅说明 ZYX 索引减起点的关系。

现有模板 `symmetries` 为空，不能直接当作 `matches_iso`。[原图元数据代码](../../process/process_torsional_info.py)包含所需自同构排列生成逻辑，只取实际需要的对称部分，不连同真实扭转角、刚体轴等一并计算。半边表示可以在内存按原顺序建立，不以预计算为由复制源图。

原纯 free 训练／验证使用空刚体域和扭转注释，dist／dih 为零。原 free 推理仍读取部分此类字段，而选定 `post_process=None` 的 free noiser 不使用其科学值；仅对本项目选定 free 路径接入兼容空字段，不改 flexible／torsional 其他任务。训练仍保留原 `matches_iso` 重分配，评价沿原 `CalcRMS`，两者的匹配口径不能互换。

### 2.5 蛋白与核酸受体特征

完整受体 `receptor_tokens.npz` 有世界 XYZ 坐标、元素、残基／链编号、atom_name 和 res_type，可从完整标准受体重新选口袋。`res_type` 0–19 为标准蛋白，20–23 为 RNA A/C/G/U，24–27 为 DNA DA/DC/DG/DT，28 为 UNK。UNK 原子不入模型或正式碰撞受体，不建立修饰母体映射，不根据旧 feat 的母体映射猜测类别。

AdaLigand 蛋白顺序为 `ALA ARG ASN ASP CYS GLN GLU GLY HIS ILE LEU LYS MET PHE PRO SER THR TRP TYR VAL`；PocketXMol 原顺序为 `ALA CYS ASP GLU PHE GLY HIS ILE LYS LEU MET ASN PRO GLN ARG SER THR VAL TRP TYR`。例如 ARG 从 1 映射到 14，CYS 从 4 映射到 1，不能直接沿用原整数。蛋白 25 维仍为元素 `[C,N,O,S]` 4 维＋原氨基酸 20 维＋backbone 1 维。

核酸特征为 float32 `(N_NA,15)`，`N_NA` 是当前口袋标准核酸重原子数：`[0:4]` 为 `[C,N,O,P]`，`[4:12]` 为 `[A,C,G,U,DA,DC,DG,DT]`，`[12:15]` 为 `[base,sugar,phosphate]`，各段独立 one-hot。糖原子为 `C1'/C2'/C3'/C4'/C5'/O2'/O3'/O4'/O5'`，DNA 无 O2'；磷酸为 `P/OP1/OP2/OP3`，兼容 `O1P/O2P/O3P`；其余合法标准核苷酸重原子为碱基。旧原子名 `*` 机械转换为 `'`，不加入修饰特征或额外 RNA/DNA 标志。

RA 独立投影后将蛋白与核酸节点合并，进入共享编码器与联合近邻图；RB 各自编码后合并上下文。RB 在官方蛋白权重加载后复制兼容参数，续训不覆盖已训练核酸分支。空核酸批次保留真实空张量，不造假节点或改变实例抽样。

历史 583 候选中 C0、E 各有 7 个标准核酸邻域；`(9v7o,0,CCD:GMP)` 无标准蛋白，完整受体 6083 原子全部是标准 RNA。C0 有 9 个、E 有 5 个选到 UNK，不能把这些数当成核酸实例数。新完整性筛选与 C5 固定偏移后重新统计，旧数字不直接进入新分层。曾核对 A1CZ2 是修饰 RNA、A1EQM/LLP 是修饰氨基酸；这个事实只说明 UNK 不能统称核酸，当前统一排除 UNK 原子。

### 2.6 原划分与已知单例交叉

现有 `stage1_preparation_box_pool_3/split/{train,validation,calibration}.json` 保存实例，`pdb_split/` 保存 PDB 分组。历史记录训练 13717 个 PDB、验证 200、校准 100。划分包含首次 EMDB 发布早于 2026-01-01、分辨率、相关系数和最小地图尺寸等源质量条件；直接继承其实例结果，不自行重划。

已按 `raw/pair_list.jsonl` 查明测试与训练、验证、校准无 PDB／EMDB 交集；训练和验证无 PDB 交集，但 `9frg/9frf` 共用 `EMD-50713`。用户已允许忽略这一个交叉，不删 9frg、不移动 9frf。历史 split 的 SHA-256 排序实现不需要重算，也不成为新 Dataset 或训练的哈希依赖。

### 2.7 训练、验证与校准的历史候选计数和 13 个坏图

以下计数产生于完整沉积重原子等新条件应用之前，保留为候选审计证据：

| 历史范围 | 原 small_molecule | 单残基 CCD 非共价 | 再符合原词表与含碳 | 候选 PDB | 候选 CCD | 记录提示原子缺失 |
|---|---:|---:|---:|---:|---:|---:|
| train | 143637 | 139553 | 91166 | 8432 | 2076 | 25988 |
| validation | 1759 | 1724 | 1308 | 125 | 144 | 527 |
| calibration | 757 | 743 | 441 | 62 | 68 | 80 |

三个 split 当时未发现重复 `(pdb_id,candidate_id)` 或所需 occurrence／模板缺失。词表排除为 46438／395／283，无碳排除为 1949／21／19。训练等缺失计数来自 `n_heavy_atoms` 与模板长度比较；测试 134 个则实际读取过 present，不能据此声称训练全表已完成逐坐标验收。

已定位 13 个训练完整图价态错误，涉及 9 个 CCD；模板 N 显式价态 4／5 与保存电荷不相容。直接按下表实例排除，不修电荷或键级：

| 训练完整图错误身份 | 数量 | 全部实例 `(pdb_id,candidate_id)` |
|---|---:|---|
| YQ1 | 2 | `(5bkj,3)`、`(8fz7,1)` |
| YQ4 | 1 | `(5bkk,2)` |
| X3P | 1 | `(7kra,3)` |
| K6G | 3 | `(7wu3,0)`、`(7wu4,1)`、`(7wu5,1)` |
| UDF | 1 | `(8e32,1)` |
| S9F | 1 | `(8txo,3)` |
| Y7Z | 1 | `(8v3g,3)` |
| K9G | 2 | `(8zto,0)`、`(8zto,2)` |
| A1EG9 | 1 | `(9kvh,0)` |

其余训练 117 个为 WJS=4、6ER=3、WUX=2、FO4=108，完整重原子图可构建但旧 SMILES 不可用。验证 72 个均为 9gkl 的 FO4，校准未发现同类问题。FO4 跨集合出现属于同一化学身份，不是同一个物理实例跨集合。

### 2.8 192 个坏 SMILES 的具体实例与重新计数

下面范围包含两端，例如 `0–7` 为八个编号。加上第 2.7 节 13 个坏图，合计 205 个历史异常实例；全部没有所需 SMI-TED 向量并记录 model_failed。当前直接排除，不修复或补算：

| 集合 | PDB | CCD | 数量 | candidate_id（范围含两端） |
|---|---|---|---:|---|
| train | 6wlw | WJS | 1 | 10 |
| train | 6wm2 | WJS | 1 | 10 |
| train | 7s4l | 6ER | 3 | 3、23、30 |
| train | 7xv3 | WJS | 1 | 0 |
| train | 8kgg | WJS | 1 | 0 |
| train | 8p2f | WUX | 1 | 63 |
| train | 8p2g | WUX | 1 | 137 |
| train | 9cc2 | FO4 | 4 | 5–6、8、10 |
| train | 9gj8 | FO4 | 32 | 0、2、9、11–17、19–20、22、30、33、35–41、43–44、46、49、57、59–63 |
| train | 9gki | FO4 | 8 | 0–7 |
| train | 9gkp | FO4 | 64 | 2、5、8、10–26、28–30、32–33、36–37、43、47、50–51、54–74、76–78、80–81、84–85、88–89、91–92、95 |
| validation | 9gkl | FO4 | 72 | 0–7、11、24–26、28–55、57–59、72、76–103 |
| test | 9oq7 | FO4 | 3 | 4、10、13 |

旧策略仅扣这些异常时曾推算 train=91036、validation=1236、calibration=441、test=580；新“沉积完整”等累计筛选后这些数全部重新计算。一个实例可能同时命中缺失与语言错误，只计一次排除。正式记录保存 `(pdb_id,candidate_id,reason)`、保留列表与统计，不在 Dataset 堆积修复分支；异常固定前排除，固定后的运行失败保留共同分母。

验证／校准历史 receptor、ligand_coords、exp、sim 文件存在；验证缺的 SMI-TED 是上述 72 个 FO4，校准候选当时均有该文件。存在性不替代向量数值、输入语义或完整几何检查，正式准备按当前累计条件完成必要预检。

## 3. 原模型、噪声和评价接口

### 3.1 官方权重与源码来源

官方权重位于 `/storage/penghongen/PocketXMol_official_test/extracted/data/trained_models/{pxm,pxm_use,tuned_ranker}/`。本地被 Git 忽略的 `model_weights.tar.gz` 为 640300872 字节；[作者 Zenodo 发布页](https://zenodo.org/records/17801271)和[官方安装说明](https://github.com/pengxingang/PocketXMol/blob/master/docs/setup.md)说明来源及解压布局，不需要重新下载。

| 历史资产 | 文件字节数 | 保存步数 | 记录的 Lightning |
|---|---:|---:|---|
| pxm/checkpoints/pocketxmol.ckpt | 243496215 | 180000 | 2.0.4 |
| pxm_use/checkpoints/pocketxmol.ckpt | 243494379 | 160000 | 2.0.4 |
| tuned_ranker/checkpoints/tuned_ranker.ckpt | 243429798 | 22000 | 2.2.2 |

选定共同起点为 pxm。其 state_dict 有 1128 项 `model.*` 与独立损失缓冲 `loss_func.cfd_loss.log_prob_1A=ln(0.2)`；模型张量元素共 20166377，含缓冲，不当作可训练参数数。模型输入维度为原子类别 12、边类别 6、蛋白特征 25。仅按 model 前缀加载模型，不能把整个 Lightning 状态当作纯模型字典。

pxm 的嵌入配置与相邻 train.yml 仅 assembly 路径不同，模型／prior／loss 一致；pxm_use 为不同训练权重，不能混用。官方完整训练 assembly 未在已查资产中找到，不能完整排除作者预训练暴露，亦不能把同 CCD 跨集合当作结构泄漏。

### 3.2 原核心固定值与当前训练的区别

| 原核心字段 | 保留值 |
|---|---|
| 口袋／配体节点／边宽度 | 128／320／96 |
| 口袋编码块／去噪块 | 4／6 |
| 配体—受体近邻数 | 32 |
| 配体元素顺序 | `[6,7,8,9,15,16,17,5,35,53,34]`，另保留原 mask 类别 |
| 原输出 | 原子类别、半边类别、坐标与三种 confidence；固定化学图不删除分类头 |
| 高斯 prior | gaussian_simple，sigma_func=sqrt，sigma_max=1.0，逐原子尺度 max(sqrt(N),1) Å |
| 信息等级 | 训练 uniform，采样 advance；原 step 不能直接作为噪声强度 s |
| 原子重分配 | reassign_in=True，保留原先后顺序和距离判据 |

作者历史训练为 lr=0.001、warmup=1000、plateau factor=0.75/patience=20/min_lr=1e-5/cooldown=10，配套 max_steps=240000，导出权重保存于 180000。作者单进程 batch=40、启动 8 GPU，无累积，故原 global batch=320。这些仅是历史，当前全部使用第 1 节新参数，不能恢复为默认。AdamW 其余参数沿用源构造，不增加分组学习率、余弦调度或新梯度裁剪。

### 3.3 free 损失与 T1 接线

free 中 `fixed_node=1`、`fixed_halfedge=1`、`fixed_pos=0`、`fixed_halfdist=0`。刚体域和扭转注释保留约定空形状；不生成真实角度或轴。原损失叶仍保留：

| 损失叶 | 原权重 | 纯 free 下含义 |
|---|---:|---|
| node／edge | 1.5／1.5 | 无待恢复类别，原实现返回零 |
| pos | 2.5 | 完整沉积重原子 XYZ 逐元素 MSE |
| dist／dih | 0.0005／0.0005 | 空刚体域／空扭转注释，原实现返回零 |
| fixed_node／fixed_edge | 0.1／0.1 | 固定化学类型仍有分类监督 |
| fixed_pos／fixed_dist | 0.1／0.0005 | 没有固定坐标或距离，原实现返回零 |

`IndividualTasksLoss` 把 confidence 追加到 `mixed/total`，不能改用缺少这些项的 `dock/total`。Confidence node／pos／edge 权重为 1／20／1；坐标目标为逐原子 `0.2 ** error_Å`，不是 RMSD<2 Å 的成功概率。保留原 sigmoid／BCE／MSE、detach 和归约；confidence 仍通过共享表示向主干回传。

已有 `noise.individual` 中dock的 `center_translation` 是中心T0/T1的唯一机制配置。`DataModule.setup` 据此为训练和监督验证选择条件：中心False用C0，中心True用C5，包络用E。`OccurrenceDataset` 只对C5训练动态抽偏移，有限C5流读取冻结向量；C0不因shuffle或center模式自动启用偏移。不新增另一套训练增强开关，不全局打开 `mol_as_pocket_center`。

令完整重原子世界坐标为 `X_star(N,3)`，几何中心为 `g(3,)`，原点为 `C(3,)`，局部目标为 `x_star=X_star-C`，单位均为Å、轴序XYZ。T0训练用C=g，mean(x_star)=0，只加原高斯。T1训练用C=g+delta，mean(x_star)=-delta；原高斯后、原同构重分配与固定字段恢复前加入同一分子共享的 `s*delta`。`s=1-level_dict['pos']`，原 `GaussianExplodePrior` 的逐原子高斯及sigma(N)不改；监督目标和受体不跟随新增平移。

中心推理始终原样使用给定c选袋并取C=c，口袋和原点在轨迹内固定。T0/T1首步均为原纯高斯先验，不再平移，也不强制实际质心归零。后续T0仅对局部预测Z加原高斯，T1再加 `s*(given_center_local-mean(Z))`；局部给定中心为0。定位完成后不读GT中心或偏移、不抽第二份delta、不截断预测中心差到5 Å；最终坐标只加回C一次。T0+C5保留不准确中心，T1+C0仍保留后续中心相关加噪，正式评价矩阵不变。

原重分配比较平均逐原子欧氏距离，而非严格 RMSD；平移可能改变最佳排列。因此共享平移验收在重分配之前进行，不要求重排后的每个原子编号仍对应同一个差向量。

### 3.4 口袋、模型原点与固定随机量

残基选择沿现有 `sample_use.py`／`PDBProtein` 的重原子质量中心判据：中心严格 <15 Å，包络到任一配体重原子严格 <10 Å。选中后保留完整残基重原子。中心T0训练按g选袋并以g为原点；中心T1按完整g+delta选袋和定原点，不能用s*delta定位。包络原点为实际选定口袋重原子世界坐标算术均值，官方只取蛋白，扩展模型取蛋白＋核酸。

C5使用半径 `r~Uniform(0,5 Å)` 和独立均匀球面方向u，delta=r*u，不是体积均匀。仅中心T1训练每次重采样一份delta；T0训练不抽新增偏移。现有验证／测试清单seed=3407生成并保存固定偏移和候选种子，采样基础seed=10831，训练seed=2023，不因本次条件修正重新冻结。训练中的原val/loss分别使用T0的C0、T1的冻结C5、包络的E；不同条件的val/loss各自选best，正式模型在共同C0/C5/E协议下比较。改变加载顺序或选择CAP10/HF10_TO5时读取同一实例记录。单轨迹不重裁口袋或密度块。

### 3.5 排名与完整受体检查

原 `self_ranking=cfd_traj+no_clashes+stereo`，两项布尔检查通过各加 1，confidence 轨迹聚合和阈值保持。官方与扩展模型的碰撞辅助检查统一使用完整标准蛋白＋RNA/DNA 受体重原子，排除 UNK、水和辅因子；生成官方仍仅接蛋白。由现有受体资产在内存装配检查器所需字段，不额外写新的受体科学文件。

原两个辅助检查不等于完整 PoseBusters。`buster_tools` 的距离判据依赖元素、PDB 元数据和排除类型，新适配须验证实际接入的受体集合；不能让名称 protein 或 HETATM 默认处理偷偷排除标准核酸／纳入辅因子。只报告实际完成检查，失败或不适用不得写成通过。tuned ranker 不加载、不适配、不补跑。

通过累计数据筛选的纯 RNA 实例仍实际尝试官方原路径。原 `FeaturizePocket` 对空蛋白可能给出 `(0,3)` center，错误可早于 forward；区分输入构造、forward、采样、还原和评价。不要临时填假蛋白、假原点，或把未进入 forward 写成模型前向失败。记录每协议口袋中蛋白／NA 原始重原子数及 `NA/(protein+NA)`；分母为零记 NA。

评价保持原不移动姿态的 `CalcRMS` 对称感知重原子 RMSD，不单独对齐配体。训练 `matches_iso` 考虑的手性置换不能代替评价原匹配。Top-1／Top-5 与全候选 oracle 分开，成功阈值严格 RMSD<2 Å。

新增逐 occurrence Spearman ρ(self_ranking,−RMSD) 和 Pose ROC-AUC，后者以 RMSD<2 Å 为正类、self_ranking 为分数；使用相同有效候选池。Spearman 报 occurrence 等权均值和中位数，AUC 报 occurrence 等权均值，不能混合不同 occurrence 候选。候选不足／恒定输入／单一类别等不可定义情况记 NA，保存原因和有效数量，不能记 0。姿态成功率另保留 occurrence 汇总和 PDB 等权汇总。

## 4. 密度几何与后续构造

### 4.1 直接裁 48 个源体素

`density/{pdb_id}/exp.npy+exp.npz` 和 sim 为 `(1,Z,Y,X)`，origin 是世界 XYZ 边界角点。直接读现有 AdaLigand canonical 资产，不重做 MRC 转换或重采样。真实 XYZ spacing 读元数据，例如历史 9lfa 约 1.000336 Å、9nvn 约 1.001739 Å，不当作 1 Å 常量。

成熟几何位于 [stage1_requests.py](../../../Pocket_Plus/src/datasets/stage1_requests.py) 的 `resolve_stage1_start`、`centered_start_from_centroid_zyx`：先 `np.rint`，后 clip 到 `[0,full_shape-box_shape]`，小于块即报错。本项目显式传 `(48,48,48)`，不用原默认 80。

设请求中心 $Q$、源边界角点 $O$、实际间距 $d$ 都按 XYZ 表示，源空间形状为 $S_{zyx}$，实际起点为：

$$
s_{zyx}=\operatorname{clip}\!\left(\operatorname{rint}\!\left(((Q-O)/d)_{zyx}-24\right),\ 0,\ S_{zyx}-48\right).
$$

先检查任一维小于 48 即报错，否则直接切三轴长度 48。保留 NumPy rint 的半整数行为，不自造 floor／trunc；不补零、不增加有效体素 mask、整块归一化。exp、所用 sim 与当前实例标签使用同一实际起点和源几何。

块内体素索引 $i$、实际起点 $s$ 重排为 XYZ 后：

$$
x_{voxel,model}=O+(s+i+0.5)\odot d-C.
$$

$C$ 沿原模型原点：中心给定中心，包络选定口袋重原子均值。请求中心 $Q$ 在中心T0训练为 $g$，中心T1训练为 $g+\delta$，中心评价为实际C0/C5给定中心，E为配体中心。裁块内缩只改变 $s$，不改变 $Q$、$C$ 或先验。此处 $s$ 是裁块起点，不是T1噪声强度；该定位约定用于后续密度阶段。

共同刚体增强作用于配体、受体和体素坐标，查询时用逆变换回到原 ZYX 数组，不旋转插值密度。D2/D4 的原子邻域越界取与块的交集，空邻域零残差，不夹边伪造密度信号。地图不足 48 的异常在固定清单前记录处理，不填假块。

### 4.2 U-Net、注入与 D3 辅助监督

D1／D4／D2／D3 分别为粗体素全局、自身邻域、原子邻域并集、概率 top-K 读出，详见科学契约。U-Net 使用 Emap2lig MUNet 或 Pocket_Plus RauUnet 的成熟实现，可在获准密度配置中固定通道数；不自动增加网络比较实验。首轮密度从 exp 单通道开始，D3 的 K 候选仍为 4096／8192，正式候选范围及预算等待用户决定。

密度先在节点更新后、边／坐标更新前注入残差，起始 alpha=0.1、门控 g=1，不同时将门控和输出投影置零。距离偏置或 attention 性能改写须比较输出及输入／参数梯度。不得跨更新复用可训练特征；冻结源数组和单次 forward 特征可以共享。

D3 使用当前 occurrence 标签和冻结的 SMI-TED 分子向量，只读预测 top-K；原 loss 上追加 `eta_seg*(0.7*focal+0.3*Dice)`。复用 [stage1_unet_c1.yaml](../../../Pocket_Plus/configs/loss/stage1_unet_c1.yaml) 及 [AdaptiveClassificationCompositeLoss](../../../Pocket_Plus/src/modules/losses.py)：两个 logits softmax，gamma=2，类别权重 0.5／0.5，全块体素平均；Tversky 两个误差系数 0.5，前景在批次和空间维归约，smooth=1，MSE 权重零。不能把相等类别权重改成 1／1 而无意放大 focal；若改为单 logit 须验等价。eta_seg 和验证选择目标在正式密度阶段前固定。

### 4.3 56 通道与 SMI-TED 来源

[density_channel_builder.py](../../../Pocket_Plus/src/datasets/density_channel_builder.py) 的通道顺序是运算外层、归一化中层、后处理内层：`OPS=[exp,sim,diff,posdiff]`，`NORMS=[nonorm,clipnorm]`，`POSTS=[nopost,gauss1,gauss2,DoG1,DoG2,smooth1,smooth2]`，共 56 通道。diff=exp−(a×sim+b)，posdiff=max(diff,0)，DoG 为 G(1.6σ)−G(σ)。高斯 reflect 边界，clipnorm 用 0.001／0.999 分位后标准化，smooth 与差图拟合使用受体区域掩码；该掩码不是边界有效体素掩码。拟合样本或 sim 方差不足的成熟回退保持 a=1、b=0。

现有完整裁块无零填充，通道函数不增加 valid_mask。历史 9lfa sim 元数据为 Chimera 1.19、strict_hetatm_removed=true，仅含所选模型 ATOM 重原子；可能漏修饰聚合物。仅披露源覆盖限制，不自动重建模拟资产。Pocket_Plus I/O 代码只作参考，不能把其整套加载结构作为新项目强制要求。

现有 SMI-TED 根为 `/storage/penghongen/AdaLigand/Ori_Data/stage1_preparation_box_pool_2/ligand_language_models/smi_ted_289m`，权重根为 `/storage/penghongen/AdaLigand/model_weights/ligand_language_models/models/smi_ted_289m/`，含 smi-ted-Light_40.pt、bert_vocab_curated.txt 与冻结源码；runtime 在同权重总根的 `runtime/smi_ted_289m/`。

[表征产物说明](../../../AdaLigand/Data_Preprocessing/Ori_Data_upgrade/ligand_language_model/README.md)规定 SMI-TED Light 289M、float32 `(768,)`、202 token 截断，输入为 canonical non-isomeric 字符串，不声称文本保留全部手性。身份、prepared_smiles、model_smiles 和向量需对应。历史覆盖记录含失败，不能把总数当可用数；失败直接排除，不补向量。SMI-TED 仅为既定 D3 概率头的条件，不扩充无密度模型。

## 5. 代码、性能与文档组织

沿现有 Dataset、模型、noiser、训练、采样和评价入口做必要小改，用显式配置区分六模型；不复制六套代码、不彻底重写或删除无关功能。新增数据适配放在职责明确的数据模块，密度网络放模型模块；不新建吸收科学定义的通用 utils，也不为旧 utils 的位置全库搬迁。

函数顺序和拆分遵守 python-writing-style-cn，不用一次转发／赋值形成薄包装，不人为凑复用次数。主代理先逐文件检查新增／修改函数的职责、位置、调用、嵌套和 Docstring，再按 code-comment-style-cn 及例子落实类／函数／代码行的形状、变量含义和科学步骤。

之后在关键实现完成点进行三类独立审查：Git／布局、注释、逻辑契约。布局例外由用户授权的布局审查代理审核批准；独立全面审查共两轮，随后只窄复核已提问题，不不断扩大范围。代理仅处理有界任务，不常驻，不让多个代理改共享入口或操作服务器资源。

三类正式新数据形成后，补必要产物 README 与实际字段例子，更新现有 talk 的输入产物概览和模块概览；日志不代替稳定接口文档。规划、执行记录和映射采用现有三文档与日志的链接，不另建重复的空文档树。

数据检查集中在准备阶段，数值／梯度／几何验收集中在非测试小样本，启动前核对展开配置和依赖；Dataset 和 forward 不逐步扫描文件或建立校验注册系统。不增加新的科学哈希体系。仅既有 release／launch 发布器内部 SHA-256 是用户明确允许的例外，Git 原版本标识和一次性等价检查可以保留，不扩展到数据、训练或结果注册。

I/O 成为训练瓶颈时必须优化，可以用内存原始数组缓存、密度 mmap 后切片及独立实例标签，但不改变抽样或增加无必要落盘数据。密度模型在同类卡上一个 global batch 耗时达到无密度的 4 倍时，必须定位读取、编码、读出或显存瓶颈；普通验收／运行中记录测量，不擅自增加实验搜索。

## 6. 服务器与正式运行

### 6.1 资产位置与授权目录

| 用途 | 路径及边界 |
|---|---|
| 当前项目源码 | `/home/penghongen/My_Project/PocketXMol`，已获准同步本项目必要改动 |
| 项目数据记录／报告 | `/storage/penghongen/PocketXMol`，保存清单、运行输出与报告 |
| 新三类预计算数据 | `/storage/penghongen/Adaligand_Build/Ori_Data`，只包含第 2.4 节获准内容 |
| 项目反馈 | `/home/penghongen/Feedback/PocketXMol`，保存新项目 release／launch／作业反馈 |
| AdaLigand 原资产 | `/storage/penghongen/AdaLigand/Ori_Data`，保持只读 |
| 原结构与地图 | 上述源根的 `raw/rcsb_mmcif/{pdb_id}.cif`、`raw/emdb_maps/emd_{emdb_id}.map.gz` |
| 实例和受体 | 源根的 `parse/{pdb_id}/occurrences.jsonl`、ligand_coords.npz、receptor_tokens.npz |
| 完整模板 | 源根的 `ligand_objects/CCD_{code}.npz`，其模板坐标不作沉积 GT |
| canonical 密度／标签 | 源根的 `density/{pdb_id}/exp.npy+exp.npz`、sim.npy+sim.npz、ligand_area.npz |
| 原 train／val／cal | 源根的 `stage1_preparation_box_pool_3/split/{train,validation,calibration}.json` |
| 官方 pxm | `/storage/penghongen/PocketXMol_official_test/extracted/data/trained_models/pxm/`，只读加载 |

权限表不表示目录均已生成，实际建立情况记录在总日志。旧 AdaLigand、Pocket_Plus、Builder 资产只读引用，八月 Builder 适配及 compat 产物不复用。

### 6.2 环境与必要兼容

已定位 `/home/penghongen/anaconda3/envs/pxm_phase1`。准备快照为 Python 3.10.20、torch 2.6.0+cu124、pytorch-lightning 2.6.0、torch-geometric 2.6.1、torch-scatter／torch-cluster 2.1.2／1.6.3、RDKit 2023.9.3、Gemmi 0.7.5、NumPy 1.24.4、PyYAML 6.0.2、LMDB 1.7.5。正式使用前核对实际环境和必要 W&B／SciPy／scikit-learn 依赖，不把历史版本表当模型验收。

实施时在获准项目目录创建 `/storage/penghongen/PocketXMol/runtime/venv`，以 `--system-site-packages` 继承上述科学依赖，仅新增固定版本的 W&B 依赖；原环境保持原状。新增依赖见[requirements-docking.txt](../../requirements-docking.txt)，运行入口统一使用此项目环境。已只读验证 W&B 账号访问，真实模型与 GPU 仍须完成验收，不能由网络连通性代替。

torch 2.6 的 weights_only 默认变化与作者 checkpoint 的 EasyDict／Namespace 有兼容点，在已确认可信权重的加载入口作必要接线；不顺带修复本轮不运行的 tuned ranker。完整 PoseBusters 包在准备快照未安装，仓库已有两项辅助检查不能冒称全套检验。实际精度、显存、速度和恢复行为在授权资源上验证。

### 6.3 CPU 与 GPU 调度

纯 CPU 准备任务每个申请 8 核，最多同时 12 个、总并发不超过 96 核；用 Slurm array 或小任务分片排队并行，多个 array 共用这一总预算，不能各自开 12 个而叠加超额。按 object_key 分配共享图任务、按 PDB 分配实例标签提取，避免并发重复写同一产物。底层数值库线程随实际配额控制，不让 worker×BLAS 线程超出分配。

GPU 默认单卡独立实验，不使用 DDP。首张获准资源是 Job 371591；更多卡仅按用户后来明确授权分配多个独立任务。单 GPU 串行训练／推理，不同模型独立输出、W&B id 和 checkpoint；名义 global 72 对每个训练模型分别成立。GPU 作业内 CPU 按该作业实际配额，已知 16 CPU 可用 15／16 个 DataLoader workers；排名原 `Pool(64)` 改为分配内 CPU 数。

每个模型训练结束后，立即用已选best完成测试集全部规定协议的候选生成、CPU评价及结果记录，再启动下一个模型。不提交训练后完整验证集采样或评价。测试推理优先batch_size=50；若更大的有效批量确能提速，可用100，每实例每协议仍只生成50候选、100步。现有单实例入口中，batch_size=100与50实际均为一批50个候选；跨实例组批需明确实现并核对实例、种子、口袋和输出归属，不能仅修改数字便声称已经提速。

9 月 9 日资源快照：371591 为 `Find_1_pdb1_resume`，RUNNING、nvlink、gnode09、1 张 A800、16 CPU；旧控制器来自 `/home/penghongen/Feedback/Pocket_Plus/task_roots/find1-pdb-centric-1-recovery-20260906/Pocket_Plus`，反馈在 `/home/penghongen/Feedback/Pocket_Plus/allocations/371591/`。接管前重新核对 job、控制器和锁，旧控制器不会因新 runtime 源码同步而热更新。

代码通过无卡／轻量 CPU 检查后，已获准用 kill_lock 接管 371591，修改必要 run_cmd 和对应 pre／try 锁，进行 GPU smoke 与正式任务。始终保留 after_lock，不 scancel／修改其他任务，不把许可扩成任意删除或释放资源。用户提过“3 张 A100”的例子不构成当前申请许可。

### 6.4 发布、W&B 与任务日志

使用[训练与运行](../../训练与运行/README.md)及[服务器交互](../../与服务器交互/other/readme.md)的通用机制，建立简短正式入口，不盲跑原 Find／U-Net 实验脚本。安全同步修正 Pocket_Plus 旧目标为 PocketXMol，并取消删除远端 .git；删除式同步不作默认入口。

既有 release／launch 内部哈希与仅限本次发布器临时目录的自动清理已明确获准，保持该有限例外；checkpoint、源数据、其他目录或任务的删除性改动仍另需明确许可。不得因脚本名为“安全同步”而扩大删除范围。

W&B 固定 entity=`pencounkdual-111`。项目 `PocketXmol_raw` 接六个无密度模型及官方对照，`PocketXmol_density` 接后续获准密度实验；可读名字如 `中心_T0_RA`、`包络_T0_RB`、`官方_C0`，阶段／seed／job_id 放配置或标签。默认 online，无法在线才 offline；恢复保留 run id，多段离线记录完整同步。API key 通过已有私有凭据或进程环境提供，不写入聊天、源码、release、配置或日志。

保留原日志键和 optimizer global_step。运行目录独立于 TensorBoard／W&B 的私有目录属性；训练目标不随日志系统改变。正式入口稳定后，在各实验日志记录实际执行的极短正式命令，不把测试／门控参数混入正式命令。

本地总记录为 `日志/总日志.md`；无密度每次训练或推理在 `日志/第一类实验（不加密度信息）/` 有独立 Markdown。训练与推理一次完成时可以同一日志，但两类命令、来源、配置、产物目录和结果分别明确。总日志串联顺序和逐实验记录；提交任务、训练完成等有意义事件再更新 handoff，不逐 epoch 写外置记忆。

任务稳定排队、正常训练或长时间加载后，由主代理按 60 分钟一轮监看，以连续 `Start-Sleep -Seconds 300` 片段组成可打断等待，保证五分钟内接收新消息；睡眠期间不发进度消息，不设置 heartbeat，不让监控代理常驻。出现报错、异常或结束时立即检查并按授权范围处理。

## 7. Git、历史与本阶段闭环

本轮实现共同基点为 `0412824adbdd4e4f229b572d9a6bfde75d3fdbc9`，开始前 Learn/CUMULATIVE 已快进到该唯一最新提交，工作区干净。实现分支为 `codex/pxm-receptor-baselines`，六实验共用此代码线；根 AGENTS 保留适用规范，项目名和文档路径按当前 PocketXMol 位置校正。

稳定后从同一基点建立 Learn 分支，先文档和契约，再核心代码与组合，全部本轮测试文件最后集中提交；若有获准删除的非测试旧文件，按双线 skill 放在最前。实现线保留真实过程，学习线只含最终行为及说明。逐路径、可执行 Python、配置、测试和关键产物验证端点等价后，再快进 Learn/CUMULATIVE，确保其按提交者时间再次唯一最新；不自动 push、不改用户原有修改、不改写运行依赖的历史。

既有 [talk/README.md](../../talk/README.md)、[学习计划](../../talk/plans/conformation_docking_learning_plan.md)和[学习记录](../../talk/logs/conformation_docking_learning_log.md)说明原核心阅读背景。旧学习基点 df1cf846、后续 d99dd521 等过程以及八月 Builder 数值复现详情保存在 Git 0412824 的本文历史版本；它们不证明新契约完成，不成为执行前置。保留资产来源和异常实例证据即可，不复用 Builder 适配、compat 数据或通过结论。

本阶段依次完成累计数据预检与三类必要CPU产物、非测试输入上的原模型／损失／坐标／训练控制验收，再逐模型完成训练、原val/loss选best及完整测试，并完成官方对照。正式报告共有13个测试组合／39个视图汇总，不包括训练后完整验证集采样。逐实例结果保存以支持复查和重新汇总。无密度完整报告是本阶段结束点；T／RA-RB的选择及密度预算交用户，未经许可不增加实验或自动开跑密度。

计划、代码、产物和运行结果的对应状态由总日志和三份文档的链接维护；本次回填只更新有效规格，不写未经运行证实的成功状态。新增实现偏差若改变科学行为，先说明影响并取得用户决定，不用负结果推动隐含调参。
