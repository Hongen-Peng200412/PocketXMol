# 密度分支实验总日志

当前状态：2026-09-14，用户已批准官方SMILES芳香键编码、bf16-mixed训练及官方FP32推理。新入口与D2/D3已集成；主代理两遍自查完成，本地集成83通过、8跳过，三类独立全面审查完成且没有生产逻辑修改项；379402于20:28开始真实GPU入口门控，尚未正式训练。371591负责中心序列，379402负责包络序列；每卡各按D1→D4→D2→D3推进，每模型训练→best测试→CPU评价完成后再开始下一项。378693专用于无密度复验，不占密度卡。

| 顺序 | 实验记录 | 中心／包络状态 |
|---|---|---|
| 1 | [D1](1-D1.md) | 等待共同验收 |
| 2 | [D4](2-D4.md) | 等待D1完成 |
| 3 | [D2](3-D2.md) | 实现与验收进行中，正式等待D4 |
| 4 | [D3](4-D3.md) | 实现与验收进行中，正式等待D2 |

所有正式命令、实际release、launch、检查点、W&B和测试指标分别写入每实验日志；当前没有已执行正式命令。每次检查取得新进度同步本表和逐实验状态；阶段完成或明确分叉再写handoff。新增或撤回GPU按用户当时授权调整独立模型分配，保持科学配置及全局批量72，不额外复制实验。

## 本次集成验收（非正式运行）

主分支从92d08d6开始，集成SMILES提交5f7bd8e、固定官方解析测试80ca5ca及D2/D3模型提交0f3ab48。主代理补D3 Dataset语言／标签、训练与val辅助日志、推理固定索引缓存；保留既定原OOM裁批和原dock损失。自查记录为本地`tmp/formal-smiles-density/main-self-review.json`。

测试命令：`tmp/pxm-20260910/venv/Scripts/python.exe -X utf8 -B -m pytest tests/test_density_dataset.py tests/test_density_selection.py tests/test_density_sampling.py tests/test_density_training.py tests/test_density.py tests/test_docking_data.py tests/test_docking_smiles.py tests/test_docking_sampling.py tests/test_docking_centers.py tests/test_docking_official.py tests/test_docking_training.py -q --basetemp=tmp/formal-smiles-density/pytest-main-2 --junitxml=tmp/formal-smiles-density/main-2.xml`。环境PYTHONPATH为项目根、OMP/MKL线程各1。首次main-1因临时父目录不存在，夹具未启动；重试main-2为83通过8跳过，158.58秒。跳过包括Windows无Flash内核和未复制的真实非测试资产，不计为通过。两项临时OOM退出检查与最终契约不符，已连同该修改撤销，后续端点检查以最终代码为准。

真实GPU入口门控已于2026-09-14 20:28:30经379402真实try_lock启动，after_lock保留。源码5af144f，证据根`/storage/penghongen/tmp/pxm_formal_smiles_20260914/density-gate`，日志`gate-5af144f.out/.err`。验收命令为`bash ops/run_docking_gpu_checks.sh -k 'real_data and (D1 or D4 or D2 or D3)' --junitxml <证据根>/training-sampling.xml`，每配置仅2优化器更新、1验证批及2候选3步；之后`python -B -m pytest tests/test_density_sampling.py`核对固定编码与重新编码。全局批量72、真实train/validation、W&B关闭。此为必要入口验收，不是正式实验，不延长已经结束的3小时I/O调优循环。
