# D3 密度实验

当前状态（2026-09-16）：尚未启动，用户已取消本轮D3正式执行，不分配资源，不自动进入训练、测试或评价。下方旧准备记录和未执行命令仅用于追溯，不构成当前授权。

本轮范围与资源终点见[密度总日志](总日志.md)；各卡完成D1、D2及指定无密度复验／官方对照后保留after_lock、资源和历史产物，在try_lock等待。

## 历史准备记录（已取消，未执行）

当前状态（2026-09-14）：已获正式授权，等待前一个密度实验中心与包络均完成；尚无正式训练结果。每个模型从官方初始参数重新训练，独立优化器与调度状态。

| 模型 | 资源 | 训练产物根 | 测试 | 当前状态／best／W&B |
|---|---|---|---|---|
| D3-C-T0-RA | 378693，gnode10，A800，16CPU | /storage/penghongen/PocketXMol/training/D3-C-T0-RA | C0/C5，50候选100步 | 尚未启动 |
| D3-E-T0-RA | 378693，gnode10，A800，16CPU | /storage/penghongen/PocketXMol/training/D3-E-T0-RA | E，50候选100步 | 尚未启动 |

所有新训练从规定官方参数初始化，重建优化器与调度状态；训练bf16-mixed，推理官方FP32张量路径。训练保留原val/loss与best选择，结束后直接完整测试，中心C0/C5、包络E；既有清单、视图、C5和种子不重建，每实例每协议50候选、100步，推理优先batch_size=50。W&B保持online，无法在线时报告，不自行切换offline。

无密度和D1起始72×1，D4/D2/D3起始36×2；OOM按72→36→24降批、累积相应1→2→3，配置global_batch_size保持72。接受原reduce_batch偶发裁批和累积梯度丢失，不增加补样或梯度补偿。仍无法运行则报告实际问题。

E使用真实配体质心定位密度裁块，模型原点仍为实际输入受体原子的算术均值；裁块内缩不改变原点。D3保持已批准辅助标签实现，不额外统计或处理相同SMILES／裁块标签差异。

D3在378693完成D2中心训练、C0/C5测试和CPU评价后启动，不等待由379403_1独立承担的D2包络。

## 正式运行命令

以下命令尚未执行；先中心训练、best测试及CPU评价，再包络完整流程。实际best产生后再记录采样和评价配置与短命令。

```bash
bash 训练与运行/sh/train_docking.sh D3-C-T0-RA
bash 训练与运行/sh/train_docking.sh D3-E-T0-RA
```

## 核查与性能记录

默认不再开展I/O改善或参数搜索，也不因D1预实验利用率未达目标自动优化。运行中观察实际I/O和GPU；只有主动发现I/O较此前可比训练变慢时才定位实际瓶颈并改善，保持实例抽样和科学定义。

前置证据见[预实验总日志](../预实验（一 --二之间）/总日志.md)。新资源明确增撤后才调整排程；完整checkpoint恢复允许重做未保存进度，保留after_lock和历史产物。

## 之前的尝试

尚无正式尝试。前置验收和性能尝试保留在预实验日志，不当作正式模型进度。
