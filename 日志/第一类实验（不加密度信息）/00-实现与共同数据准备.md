# 无密度首轮：实现与共同数据准备

依据 [科学契约](../../想法/方案草稿/9-8-科学契约.md)、[工程细节](../../想法/方案草稿/9-8-工程与实现细节.md) 和 [边界清单](../../想法/方案草稿/9-8-边界与核查清单.md) 实施共同数据与六模型运行链。本记录覆盖代码、共同 CPU 准备和正式实验之前的验收；各模型训练与推理另记独立日志，正式密度训练不在本阶段内。

## 授权与起点

- 2026-09-10，用户明确授权建立 goal 并端到端实施。CPU 每任务申请 8 核，总并发不超过 96 核；更多 GPU 依照后续明确授权。
- 共同 Git 基点为 `0412824adbdd4e4f229b572d9a6bfde75d3fdbc9`，提交名 `9-10_1`。检查了全部本地及远端跟踪引用和已登记工作树，`Learn/CUMULATIVE` 指向唯一最新提交，工作区与暂存区干净。
- 实现分支为 `codex/pxm-receptor-baselines`，从上述累计基点创建。学习分支待代码稳定后从相同基点重建，不推送远端。
- 规格来源为三份 `.review/*17889734*_aicmd.md` 批注及后续回复。15 维核酸编码、统一完整标准蛋白＋核酸碰撞检查、仅保留既有发布器内部哈希例外已得到明确答复。
- 原资产 `/storage/penghongen/AdaLigand/Ori_Data` 只读；三类必要派生数据可写 `/storage/penghongen/Adaligand_Build/Ori_Data` 专用目录。
- 获准项目目录为 `/home/penghongen/My_Project/PocketXMol`，输出为 `/storage/penghongen/PocketXMol`，反馈为 `/home/penghongen/Feedback/PocketXMol`。371591 的接管、run_cmd 和对应 pre/try 锁操作已有许可；保留 after_lock，不改其他任务，不自动清理 checkpoint。

## 已完成工作

1. 重新阅读双线 Git、技术表达、Python 布局、代码注释及例子、文档规范，以及服务器交互与计划日志治理规则。
2. 建立明确 goal；核对 Git 起点后创建实现分支。
3. 开始回填三份当前规格，更新项目入口及本组执行记录。
4. 三份当前规格已完成批注回填，13 个坏图和 192 个坏 SMILES 的原编号表保留为历史证据。代码接入原 PocketXMol；没有复用八月 Builder 的实现或验收结果。
5. 新增 `docking/assets.py`、`dataset.py`、`preparation.py` 和共同准备入口；仅保存共享 `matches_iso`、逐实例源 ligand-area 索引及清单／诊断，不复制基础图和坐标。已建立六份明确训练配置。
6. 只读核对原语言资产：GMP 的源 SMILES 和已编码 `prepared_smiles` 有 Kekule／芳香写法差异，不能以逐字相等作筛选；改按已有语言模型使用的无手性规范身份比较，不改源字符串或向量。
7. 只读取得服务器 Lightning 2.6.0 的调度、验证、checkpoint 与恢复方法。训练回调在保存前调用原 Plateau 一次，保存该次下降次数和停止状态；没有改写优化器或原 loss。普通续训核对完整科学配置，只允许已批准的资源和日志偏好变化。
8. 建立 RA/RB、T1、原训练控制和原采样／评价的必要接线。采样保存实际执行阶段与模型调用进入／返回次数；存储读取错误直接报错，避免把环境故障永久记成科学失败。完成三类两轮独立审查及注释窄复核，尚须运行验收。
9. 安全同步入口固定项目目录和已知主机密钥检查，保留远端 `.git` 与现有文件。已执行第一次安全同步，发送约1.69 MB；后续审查修正尚须再次同步。未运行删除式同步。
10. 在服务器新项目目录建立继承现有 `pxm_phase1` 的虚拟环境，补充 W&B 0.21.1 及其依赖，原科学环境未改动。W&B API 对 `pencounkdual-111` 的只读账号访问通过；目标项目将在正式运行时创建，尚未创建正式 W&B run。没有读取、输出或保存密钥。
11. 8核CPU验收作业 `376476` 已在cnode01完成，退出码0，24项通过、耗时58.42秒。排队期间也在本地项目 `tmp/pxm-20260910/venv` 建立隔离环境继续验收；原本地环境未改动。

## 正式运行命令

尚未执行正式 CPU 准备、训练或推理。稳定入口完成后在此记录实际正式命令、release、launch、作业编号和产物根；不得以检查命令代替正式运行记录。

## 只读检查与测试命令

以下属于检查，不是正式实验：

```powershell
git status --short
git rev-parse HEAD Learn/CUMULATIVE
git worktree list --porcelain
git log --all --format='%ct %H'
```

检查结果：起点一致，只有项目根一个工作树，未发现用户未提交修改。

## 自查与独立审查

共同数据链已完成主代理两遍自查，范围为 `docking/assets.py`、`dataset.py`、`preparation.py`、`scripts/prepare_docking.py` 和原 `ConfTransform` 的两个修改方法。第一遍逐函数核对职责、位置、调用、嵌套及 Docstring，移出局部 import、收窄 CPU worker 传参、修正分片说明；第二遍核对形状、索引、世界／模型坐标、重原子完整性、对称与语言身份语义，补齐 `docking/README.md`，同步修正 free 无几何分支注释。AST 解析、新增模块注释标点及 `git diff --check` 通过。构造资产测试正在补齐依赖后运行，不能以静态检查当作运行通过。

随后对该链提交独立审查，现已完成两轮及写盘异常等具体问题的窄复核。训练和采样链另经主代理两遍自查，再按布局与 Git、注释、科学逻辑三类职责完成两轮全面审查，之后仅窄复核。函数布局具名例外的批准权由用户交给布局审查代理。

共同数据第一轮修正了派生图与逐实例索引的写盘异常边界：输出失败直接传播，不能进入科学排除清单；补充故障测试。另修正官方空口袋坐标形状、完整模板字段、free 分支注释及 README 中清单字段和核酸组名。运行说明已改为本项目的实际入口。审查者批准 `select_pocket`、`read_occurrences`、`prepare_object`、`prepare_pdb`、`prepare`、`OccurrenceDataset` 三个方法和 `ConfTransform` 两个修改方法的具名布局例外。第二轮及窄复核无剩余静态问题。

主代理另完成训练、模型和采样评价链两遍自查。第一遍按文件顺序检查 `PMAsymDenoiser`、`ConfSampleNoiser`、`DataModule`、`ModelLightning`、`DockingCheckpoint`、采样与评价入口的职责、参数、调用次序及异常边界；核对 RB 在官方加载后的复制、T1 在重分配之前的平移、验证调度与保存顺序。第二遍逐项核对类、函数和关键代码行的形状、编号、坐标与产物说明，修正字段重字，明确主进程 RNG 与 worker 预取序列的恢复边界。三类独立审查均已完成两轮，提出的问题已修正并窄复核通过，包括恢复配置、候选池身份、原采样阶段、验证只汇总ALL，以及置信度和轨迹注释。GPU与官方权重的实际验收仍未完成。

## 环境、同步与验收留证

这些命令是环境准备或测试，不是正式实验。第一次安全同步在本地项目根执行：

```powershell
& '.\与服务器交互\sync_code.ps1'
```

服务器项目虚拟环境位于 `/storage/penghongen/PocketXMol/runtime/venv`，通过 `--system-site-packages` 继承 `/home/penghongen/anaconda3/envs/pxm_phase1`；新增依赖版本记录在根目录 `requirements-docking.txt`。基础环境为 Python3.10.20、Torch2.6.0+cu124、Lightning2.6.0、PyG2.6.1、RDKit2023.9.3、NumPy1.24.4、SciPy1.10.1、scikit-learn1.1.0。环境安装临时目录为 `/storage/penghongen/tmp`。

CPU验收作业376476的实际提交命令为：

```bash
bash 训练与运行/submit_task.sh --sh ops/run_docking_checks.sh --resource cpu --cpus 8 -- tests/test_docking_data.py tests/test_docking_model.py tests/test_docking_training.py
```

作业376476实际release为 `/home/penghongen/Feedback/PocketXMol/releases/PocketXMol_a4f27ba4b74f/PocketXMol`，launch为 `/home/penghongen/Feedback/PocketXMol/launches/376476/run_docking_checks_job376476_20260910T031504_a1`。out／err保存在同反馈根 `allocations/376476/`。它执行的是第一次同步内容，24项均通过；不包含之后增加的采样评价及恢复配置检查，后者需另验收。371591资源尚未触碰。

本地临时环境继承 `D:/Anaconda/envs/Pocket_Plus_windows`，补充RDKit2023.9.3、PyG2.6.1、NumPy1.24.4、EasyDict1.13、W&B0.21.1、PeptideBuilder1.1.0、LMDB2.3.0、scikit-learn1.1.3和openbabel-wheel3.1.1.23。它仅用于构造资产CPU测试；本地Lightning2.6.1和SciPy1.11.4与正式服务器略有差异，不能替代服务器验收。临时环境和pytest产物只通过本地 `.git/info/exclude` 排除，不进入代码提交或同步。

本地实际运行发现PROTEIN_CLASS的NumPy默认整数在Windows为int32，原one_hot需要int64；已在常量定义显式固定dtype，类别顺序与Linux数值不变。旧配置读取器使用系统默认编码，本地测试通过 `PYTHONUTF8=1` 运行，不为Windows重写原读取器。修正后数据、模型及原训练控制24项通过，新增普通恢复配置与采样评价9项通过，共33项。各阶段pytest临时产物保留在 `tmp/pxm-20260910/pytest-local-06` 和 `pytest-local-07`。

本地测试使用以下环境变量及命令，第二条是读取器编码确认后的窄复测：

```powershell
$env:CUDA_VISIBLE_DEVICES=''
$env:OMP_NUM_THREADS='1'
$env:MKL_NUM_THREADS='1'
$env:OPENBLAS_NUM_THREADS='1'
$env:PYTEST_DISABLE_PLUGIN_AUTOLOAD='1'
$env:PYTHONUTF8='1'
& './tmp/pxm-20260910/venv/Scripts/python.exe' -B -m pytest -q -p no:cacheprovider --basetemp tmp/pxm-20260910/pytest-local-06 --tb=short --disable-warnings --maxfail=4 tests/test_docking_data.py tests/test_docking_model.py tests/test_docking_training.py tests/test_docking_sampling.py
& './tmp/pxm-20260910/venv/Scripts/python.exe' -B -m pytest -q -p no:cacheprovider --basetemp tmp/pxm-20260910/pytest-local-07 --tb=short --disable-warnings --maxfail=4 tests/test_docking_training.py::test_cli_rejects_resume_with_changed_science tests/test_docking_sampling.py
```

第一条实际运行时尚未设置PYTHONUTF8，得到24通过和4个配置解码错误后停止；第二条设置后新增9项全过。不得把第一条记成全套一次通过。5个新增shell入口逐文件 `bash -n` 通过，`git diff --check` 通过。

## 计划与实现差异

尚未完成服务器正式准备与模型验收。新批注将 CPU 单任务由计划中的 16 核改为 8 核，已作为当前规格执行。正式密度实验仍处于用户查看无密度结果后的选择节点，全部模型训练及评价也尚未完成。
