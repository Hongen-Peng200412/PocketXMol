# 非密度实验收口记录

依据：[科学契约](../../../想法/方案草稿/9-8-科学契约.md)、[工程细节](../../../想法/方案草稿/9-8-工程与实现细节.md)、[边界清单](../../../想法/方案草稿/9-8-边界与核查清单.md)，以及用户于2026-09-14批准的RA＋T0选择。本记录落实当前代码与说明的收敛，保留六个实验全部历史；密度实现与正式密度运行不在本次范围。

当前状态：RB独立受体图／编码器与T1中心偏移／附加平移已从活跃代码和配置退出，详细日志迁移及当前契约更新完成。首轮完整CPU回归43项通过，1项旧服务器准备资产检查因本地没有完整源根而跳过；本次新增真实训练实例的中心／包络官方比较均通过。正在补充C5严格边界检查及独立审查、双线Git收口。此次没有服务器写入、作业操作、删除服务器产物或重新训练。

## 来源与落盘

- 共同基点：`463d59098bca92b7278839992afd0efc84d5db81`，开始时Learn/CUMULATIVE指向按提交者时间唯一最新提交，工作区及暂存区干净。
- 实现分支：`codex/no-density-closure`。旧六模型实现另保存在`codex/pxm-receptor-baselines`的`84367b241b9b572f0e840f2273b7f3993754bbf2`，两者端点内容已由上一轮核对为相同。
- 旧RB/T1配置通过共同基点中的`configs/docking/`追溯；本次只删除本地活跃配置，不修改各实验真实运行时保存的配置。
- 本次临时脚本和验收证据：`tmp/no-density-closure/`，仅用于本次整理，不被正式代码导入。
- 详细总日志已移至本目录`总日志.md`；根`日志/总日志.md`改为全项目阶段索引。七个实验日志除链接目标外的正文逐字核对不变，全部数字表格、实际命令和失败历史保留；246个修改文档中的本地链接通过。

## 验收命令与结果

官方参照是本地`upstream`所指作者仓库`https://github.com/pengxingang/PocketXMol.git`的明确提交`65488cf635c856101dbe703ac97e2f10f58e005c`。验收直接从Git对象执行该版本的先验、信息等级、噪声器、采样循环与解码，同时与收口前正确T0版本463d590比较，不以当前函数对自身比较代替参照。

同口袋、局部输入、信息等级和随机数下，训练／采样噪声、原同构重分配及fixed恢复、C0/C5的100步循环全部输出和轨迹逐值一致；原先验与等级映射各个类／函数的可执行AST一致。唯一布局差异是学习历史把GaussianExplodePrior定义提前，不改变定义内容或调用。RA在纯蛋白、混合及纯核酸输入中的参数键、前向和梯度与463d590逐值一致。GT隔离和单次世界坐标还原由真实采样至SDF路径核对。

真实非测试来源为冻结`train/5ftl/0/CCD:ADP`。仅通过SSH读取该训练记录、源ligand_coords.npz、receptor_tokens.npz、CCD_ADP.npz及既有对称排列，未写服务器或重新准备资产。来源、字节数与副本校验值在`tmp/no-density-closure/train-provenance.json`，本地副本在`tmp/no-density-closure/train-assets/`。为DataModule构造器提供的本地validation.jsonl为空文件，不用该训练实例计算验证损失。

以下均在项目根PowerShell执行，属于验收，不是正式运行：

```powershell
$env:OMP_NUM_THREADS='1'
$env:MKL_NUM_THREADS='1'
$env:OPENBLAS_NUM_THREADS='1'
$env:CUDA_VISIBLE_DEVICES=''
$env:PXM_ACCEPTANCE_ASSETS=(Resolve-Path 'tmp/no-density-closure/train-assets').Path
& 'tmp/pxm-20260910/venv/Scripts/python.exe' -X utf8 -B -m pytest tests/test_docking_data.py tests/test_docking_model.py tests/test_docking_training.py tests/test_docking_sampling.py tests/test_docking_centers.py tests/test_docking_official.py -q -x -p no:cacheprovider --basetemp=tmp/no-density-closure/pytest-main --junitxml=tmp/no-density-closure/main.xml
```

结果保存在`main.log`、`main.xml`：43 passed、1 skipped，130.59秒。先前两次调试失败均来自验收脚本：把嵌套字典当数组比较，以及把不改变行为的类定义顺序当执行差异；分别改为逐字段及逐定义比较，没有修改科学实现。真实数据、原始版本和当前版本未发现T0科学不一致。

文档核查命令为`tmp/pxm-20260910/venv/Scripts/python.exe -X utf8 -B tmp/no-density-closure/check_docs.py`，同时验证七日志正文与详细结果表不变。独立审查和两端最终验收完成后补充其证据。当前任务没有提交GPU门控，也未声称本地CPU检查等于新的GPU训练验收。

## 正式运行命令

本次未执行正式训练、采样或评价命令。各历史实验的正式命令仍在各自实验日志中；本次验收命令不能作为正式运行记录。

## 自查、独立审查与Git

主代理已经完成两遍自查：按文件顺序核对10个原有方法的职责、位置、调用、嵌套和Docstring，无新增正式函数或包装；第二遍核对类／函数／代码行的原子与图维、XYZ／Å、C0/C5定位、RA共享编码、原噪声与重分配顺序，并规范修改方法内的注释标点。范围记录在`tmp/no-density-closure/main-self-review.json`。独立两轮全面核查完成后补充结论，此后只复核已经提出的问题。

最终收口提交信息为`————— 非密度实验收口 ——————`，学习线与实现线通过等价验收后再快进Learn/CUMULATIVE。最终两端哈希及快进状态将保存于`tmp/no-density-closure/final-git.json`；只有该记录明确为complete且引用已核对的验收结果时，才表示Git收口实际完成。

## 计划与实现差异

当前变化来自用户明确决定：后续固定RA＋T0，旧RB/T1只作为历史；C0/C5仍是同一T0入口的不同定位输入。未新增科学实验。密度结构、性能预改善安排和资源增减策略留待收口后的讨论与授权。
