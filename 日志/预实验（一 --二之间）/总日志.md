# 密度正式训练前的集成收口

2026-09-14正式授权更新：本阶段已收口，用户接受已披露GPU验收限制，不重跑整套预实验。后续当前记录见[密度正式总日志](../第二类实验（密度分支的训练）/总日志.md)及[无密度复验总日志](../RA-T0第二次严格消融/总日志.md)。下文暂停等待、旧资源分工与性能目标均为前置阶段当时记录。

当前状态：2026-09-14，精确SMILES迁移及D1/D4/D2/D3前置集成已经完成代码、非测试GPU检查和三类独立审查；双线端点以最终收口证据为准，两端点验收已取得结果。正式训练、完整测试及评价均未启动。按用户最新要求，本次只收口前置集成并汇报，结束本阶段goal；三张A800保留after_lock，后续运行必须再次获得用户明确允许。

## 当前结果与限制

2026-09-14前置集成收口：实现与学习候选的同范围本机检查均为85通过、8跳过（分别198.96秒、288.59秒）；必要真实D3-C入口在实现端已通过，学习端另外实际运行1通过、0跳过，112.07秒。学习端launch为`learning_d30a132_D3C_gate`，输出`density-gate/learning-training-sampling.xml`，本地完整记录`tmp/formal-smiles-density/learning-gpu-evidence.json`。这次只重跑同一真实小范围，没有正式运行。

| 项目 | 已取得的证据 | 结论边界 |
|---|---|---|
| SMILES迁移 | 66878实例的化学图与坐标映射通过；1979/1979精确SMILES与固定官方解析的元素、键端点及类别一致 | 28650个实例相对旧ligand_object接入的键类别差异来自旧接入未使用官方芳香键类别，不能算成新链不支持，也不能继续以旧结果作为新编码基线 |
| 官方T0 GPU检查 | 3个训练实例×C0/C5/E共9输入：离散图、原点及局部坐标相同；FP32单步前向和梯度达到既定门限；900个同状态噪声／回写步骤精确相同 | 支持图输入及T0机制实质对齐；AdamW只记录差值与有限性，未证明逐参数等价；6baj/14 E第84步严格重复前向失败，已定位原GPU归约及等距kNN邻居切换，未隐藏失败或改变原公式 |
| 八配置真实入口 | D1/D4/D2/D3×中心／包络，真实train/validation、bf16更新、短采样与CPU评价：8通过、0跳过 | 每配置2次更新、1验证批、2候选3步；验证／采样用5irx/0，不能当作正式结果或持续性能 |
| 密度推理缓存 | 四类结构×sdpa/flash：8通过、0跳过 | 检查固定编码与重新编码、D3固定索引和真值隔离，不扩大成正式100步密度效果测试 |
| 持续供数 | 三小时共24次尝试结束。D1 72×1：热态1.965708秒/更新、等待29.2087%、GPU均值64.92%；D4 36×2：4.571123秒、等待0.018913%、GPU均值96.07% | D1三个窗口没有全部达标；D4三个窗口GPU中位数均100%。D4 72×1反向OOM，保留失败，不再延长本次调参 |

详细证据分别见[SMILES日志](1-SMILES构图与GPU验收.md)和[密度I/O与GPU日志](2-密度I_O与GPU性能.md)。预实验产物与临时门控全部保留；历史六模型与官方对照继续保存在[第一类实验总日志](../第一类实验（不加密度信息）/总日志&分析/总日志.md)，不改写旧指标。

## 新数据与源代码位置

新入口只从精确SMILES及公共图构造配体，使用同一顺序的沉积坐标；图资产为`/storage/penghongen/AdaLigand/Ori_Data/smiles_assets/smiles_graphs_v1.npz`，排列为同根`smiles_symmetries_v1.npz`，坐标为`SMILE_coords/v1`，迁移清单为`/storage/penghongen/PocketXMol/data/smiles-v1/frozen`。没有重新生成完整图、冻结C5或对称排列。D3冻结语言向量读取原`stage1_preparation_box_pool_2/ligand_language_models/smi_ted_289m/<pdb>/<candidate_id>.npz`，核对精确prepared_smiles；标签只进入训练及监督验证，不进入模型推理。

共同Git基点为92d08d6db737009b6c5af9b7e91be2283d85a7f6，实现分支`codex/smiles-density-formal`保留真实集成过程，学习分支`Learn/smiles-density-integration`按文档、数据、模型、组合和最终测试排列。两个端点、全树比较和同范围检查统一保存到`tmp/formal-smiles-density/dual-endpoints.json`；只有验证后才能快进Learn/CUMULATIVE。临时诊断和GPU失败状态仅在tmp及服务器证据目录，未加入活跃生产入口。

## 自查、独立审查与验收命令

主代理已逐文件完成两遍自查：先检查新增及修改函数职责、位置、调用关系、嵌套和Docstring，再统一中文职责、数据形状、变量语义及科学步骤。证据`tmp/formal-smiles-density/main-self-review.json`。布局/Git、注释、科学逻辑三类代理各完成一轮全面审查，其后只复核已提出问题。布局代理批准SMILES资产缓存／坐标切片函数及DensitySelection模块的具名布局例外；说明中的公共SMILES顺序、D3语言资产字段、历史输入偏差及授权状态已修正，未提出待修生产科学逻辑。

最终本机实现端点同范围验收为85通过、8跳过、198.96秒，记录`tmp/formal-smiles-density/implementation.xml`。跳过项是本机未提供的Flash内核或真实非测试资产；实际A800检查单独记录，不把跳过计为通过。学习端点使用相同命令与独立输出目录，85通过、8跳过、288.59秒，记录`tmp/formal-smiles-density/learning.xml`。5项跳过为本机Flash不可用，3项为服务器非测试资产未复制。

双端点必要真实检查固定为现有`test_real_data_training_and_sampling_budget[D3-C-T0-RA]`，布局/Git代理窄复核认可该范围。实现端使用上述八配置中的D3-C实际通过记录；学习端绑定与最终源逐文件一致的不可变release，再独立执行同项，单独保存`density-gate/learning-training-sampling.xml`。它覆盖真实批量72、两次更新、原val/loss及D3辅助项、C5短采样与CPU评价，不重跑其他七配置；本机两端点仍分别执行相同完整范围。源文件对应、两次实际运行及XML结果统一纳入`dual-endpoints.json`，不把同一次GPU运行冒充两次。

测试命令（非正式运行）：`tmp/pxm-20260910/venv/Scripts/python.exe -X utf8 -B -m pytest tests/test_density_dataset.py tests/test_density_selection.py tests/test_density_sampling.py tests/test_density_training.py tests/test_density.py tests/test_docking_data.py tests/test_docking_smiles.py tests/test_docking_sampling.py tests/test_docking_centers.py tests/test_docking_official.py tests/test_docking_training.py -q --basetemp=tmp/formal-smiles-density/pytest-implementation --junitxml=tmp/formal-smiles-density/implementation.xml`。PYTHONPATH为对应工作树，OMP_NUM_THREADS=1、MKL_NUM_THREADS=1、PYTHONDONTWRITEBYTECODE=1。GPU命令、源提交、release、launch及XML见密度逐实验日志；SMILES900步和严格失败证据见SMILES逐实验日志。

环境准备命令（已执行，非测试或正式运行）：`/storage/penghongen/PocketXMol/runtime/venv/bin/python -m pip install --no-deps einops==0.8.1`。只安装在PocketXMol专用venv，已核对导入位置，公共Conda未改动。密度ALL配置保留原56通道代码，5类48³输入与Pocket_Plus原函数逐元素比较通过；没有recycle。

## 待再次批准的后续执行计划

| 资源 | 拟执行模型顺序 | 每个模型的完整流程 |
|---|---|---|
| 371591，gnode09，A800，16CPU | D1-C → D4-C → D2-C → D3-C，全部RA＋T0 | 官方参数初始化训练 → 原val/loss最低best的C0/C5完整测试 → 本资源CPU评价与汇总，再开始下一模型 |
| 379402_0（实际379402），gnode09，A800，16CPU | D1-E → D4-E → D2-E → D3-E，全部RA＋T0 | 官方参数初始化训练 → best的E完整测试 → 本资源CPU评价与汇总，再开始下一模型 |
| 378693，gnode10，A800，16CPU | B-C-T0-RA-SMILES → B-E-T0-RA-SMILES → official-SMILES | 两个无密度模型分别重新初始化并训练、best测试及CPU评价；随后官方冻结权重C0/C5/E测试及评价，无官方模型训练 |

三张卡可并行执行独立模型，同一卡按上表顺序。密度D1起始72×1，D4/D2/D3起始36×2；无密度72×1。名义全局批量72，沿用原OOM裁批行为；正式训练bf16-mixed，推理FP32张量并保留原medium矩阵设置。从官方pxm参数重新建立优化器和调度状态，不继承旧无密度或门控的模型状态。

训练沿用当前三份契约：AdamW初始lr=1e-4、warmup=0，每800次优化器更新计算原val/loss；ReduceLROnPlateau相对阈值1%、patience=5、factor=0.2，第三次实际下降停止，上限40000更新。best按最低原val/loss选择，last保留完整恢复状态，D3辅助损失另记且不改变原val/loss选best。每实例每协议50候选、100步；C0/C5共用T0采样入口，E沿用包络。推理batch_size=50，当前每实例候选池只有50个，单纯改成100不会增加实际组批；不为此新增推理框架。训练后直接测试，不安排完整验证集采样。

正式开训初期、超过预取容量后的稳定窗口、运行一段时间后和换卡后分别记录I/O、等待、GPU利用率、显存及完整更新时间。D1未达目标是明确待改善项；后续先复查实际主要瓶颈，在不改变实例均匀抽样和科学构造的范围内改善，不重新启动本次三小时网格搜索。新资源加入或撤回按用户届时授权调整未开始模型或从最近完整last恢复，接受重做未保存的验证间隔，保留after_lock及旧产物，不自行删除或取消其他作业。

新的密度产物根为`/storage/penghongen/PocketXMol/training/<D_i-C/E-T0-RA>`，无密度复验使用独立`B-C-T0-RA-SMILES`、`B-E-T0-RA-SMILES`目录及新W&B，官方新结果为`/storage/penghongen/PocketXMol/sampling/official-SMILES/test`。密度每D_i一份日志包含两模型；复验继续原第1、第3、第7实验日志，失败及中断追加在对应文件后半，不另建尝试日志。每次取得新进度同步该实验及所属总日志，阶段完成或分叉才写handoff。

## 正式运行命令（计划，全部尚未执行）

训练短入口例如`bash 训练与运行/sh/train_docking.sh D1-C-T0-RA`、`bash 训练与运行/sh/train_docking.sh D1-E-T0-RA`和`bash 训练与运行/sh/train_docking.sh B-C-T0-RA-SMILES`。其余只替换上表模型名；实际release、launch、开始时间及W&B在获准启动时记录。训练完成后读取真实best再固定采样配置，使用`bash 训练与运行/sh/sample_docking.sh <模型测试配置名>`及对应`evaluate_docking.sh`。官方已备好`bash 训练与运行/sh/sample_docking.sh official-SMILES-test`和`bash 训练与运行/sh/evaluate_docking.sh official-SMILES-test`。这些计划入口不能写成已经执行，门控命令不能作为正式命令。

## 资源保留与后续入口

当前goal已只读确认是paused，保持暂停，不将未启动的正式实验标为完成。验收取得最终结果后仅回填本次文档，再从共同基点重建最后学习提交；生产源码、配置和测试与实际运行版本完全相同，精确对应纳入端点证据。

371591的控制目录为`/home/penghongen/Feedback/Pocket_Plus/allocations/371591`；379402为`/storage/penghongen/Adaligand_infered_receptor_data/cryoatom2/calibration/运行日志与统计/slurm/allocations/379402`，两者20:48以后核实after_lock和父try_lock存在，kill_lock不存在，控制器等待，指定GPU各0%／5MiB。378693控制目录为`/home/penghongen/Feedback/Pocket_Plus/allocations/378693`；Matcher进程组13179已经按用户授权经kill_lock停止，原命令及检查点保留，接管证据在`/storage/penghongen/tmp/pxm_formal_smiles_20260914/takeover-378693`。收口再次核对三资源，最终快照保存在`tmp/formal-smiles-density/`。没有scancel、删除旧资产或启动任何正式模型。

全项目见[阶段索引](../总日志.md)，正式密度计划见[第二类实验总日志](../第二类实验（密度分支的训练）/总日志.md)，依据为[科学契约](../../想法/方案草稿/9-8-科学契约.md)、[工程细节](../../想法/方案草稿/9-8-工程与实现细节.md)及[边界清单](../../想法/方案草稿/9-8-边界与核查清单.md)。

## 之前的尝试：上一阶段收口快照

以下为此前三小时预实验与旧接入的当时记录，保留其命令、分支、结果及未完成判断；当前状态以前部为准。

# 密度正式训练前的两项预实验

当前状态：2026-09-14官方解析补查。公共SMILES图与固定官方解析函数在全部1979个精确字符串的元素、键端点及类别上完全一致，覆盖66878实例；42.84%差异来自旧ligand_object接入保留原始单双键，与官方芳香键编码不同。SMILES新链已集成并完成9个非测试输入的官方对齐检查；原GPU等距kNN重复性导致的严格数值失败保留，结论限定为图输入和T0机制实质对齐。密度预实验已在三小时内结束全部24次尝试，D4持续供数达标、D1未达标。正式训练、测试与评价尚未启动。三类独立全面审查已完成；SMILES检查的通过、只测量与严格失败分项见逐实验日志。20:28在379402提交四类密度模型真实入口门控；修正发布副本的shell换行后，D1中心／包络训练、短采样和CPU评价均通过，D4/D2/D3继续，未启动正式模型。

官方作者权重包的训练配置为bf16-mixed，官方推理使用FP32张量且继承medium矩阵乘精度设置。历史六模型及官方冻结权重对照都使用旧配体编码，其结果全部保留，但不能将新旧差异仅归因于密度。用户已批准在新增A800 378693上完成修正编码的无密度中心／包络RA＋T0重训和官方冻结权重C0/C5/E测试；371591与379402继续专用于D1→D4→D2→D3。依据及精确范围见[SMILES日志的官方补查](1-SMILES构图与GPU验收.md#官方解析与精度的补充核查)。

## 结果与后续入口

| 预实验／模型 | 已确认结果 | 当前处理 |
|---|---|---|
| SMILES构图与GPU验收 | 化学图／坐标映射检查66878实例通过；公共图与官方离散解析1979/1979通过；28650例是相对旧接入的键类别差异 | 代码已集成，9输入900状态的官方噪声/回写精确相同；前向/梯度通过、AdamW仅测量、等距kNN严格失败分别记录，独立审查认可图输入/T0实质对齐 |
| D1密度模型 | 72×1、32加载进程；60次完整更新的热态均值1.965708秒，数据等待29.2087%，GPU平均64.92% | 当前最快已验证常规配置；持续供数未达目标，不能用1.37秒短计算窗口作为正式持续吞吐 |
| D4密度模型 | 36×2、32加载进程；48次完整更新的热态均值4.571123秒，等待0.018913%，GPU平均96.07% | 本窗口达到供数目标；72×1在反向OOM，不能使用 |
| 包络训练入口 | D1-E、D4-E分别完成4次真实加载、前向、反向和AdamW更新，参数有限 | 属于入口短验收，不作为持续I/O证明 |

详细结果、运行来源和全部尝试分别见[SMILES预实验](1-SMILES构图与GPU验收.md)与[密度性能预实验](2-密度I_O与GPU性能.md)。全项目阶段入口见[总日志](../总日志.md)，既有非密度事实仍在[第一类实验总日志](../第一类实验（不加密度信息）/总日志&分析/总日志.md)。

超过预取容量后的三个连续窗口：D1第33—42、43—51、52—60次更新，GPU中位数为41.5%、35%、92%，等待为50.54%、48.36%、0.0375%；前两段未达标。D4第17—27、28—38、39—48次更新，GPU中位数均为100%，等待为0.02086%、0.02214%、0.01878%；三段均达到本次目标。正式开训初期、运行一段时间后和换卡后仍须复查，短预实验不能保证长期表现。

最后第24次仅尝试当前进程的MADV_RANDOM读取提示，60更新均值1.842350秒、等待26.1476%，三个稳定窗口仍未全部达标；不同实例前缀与共享页缓存使小幅差异不能证明独立收益。因此未集成正式读取逻辑。密度通道模块保持原实现，调用原默认`enabled_channels=["all"]`展开56通道；与Pocket_Plus的5类48³输入逐元素比较通过。

距离项按已批准的完整更新开销规则保留：D1增时0.9659%，D4差异-0.2326%，均不超过10%。四份D1/D4配置已固定当前批量、32加载进程、prefetch_factor=1、cudnn_benchmark=true和distance_bias=true。D2/D3正在隔离分支实现并执行独立数值检查，尚未完成集成验收；不把D1/D4验收外推给它们。

## 必要验收与源代码

- 主代理两遍自查、一轮独立全面审查及问题窄复核已完成；布局审查代理已批准具名结构例外。科学审查发现的Flash／FP32推理问题已修复，未改原T0噪声、loss或置信度。
- A800上D1/D4×sdpa/flash的真实采样检查为4通过、0跳过、19.43秒，涵盖固定编码缓存与逐步重新编码。源提交0a9c849，release为source_2b2c8cee0d7e，原始日志为远端任务根的`root_sampling_gate_0a9c849.log`；主代理及科学审查代理分别只读核实。
- 批量读出8组合输出与梯度门控通过，最大输出绝对差2.77157e-5、最大非零梯度相对RMS差0.00688379，参考为明确记录的highest FP32数学实现，不声称与旧bf16逐元素相同。
- 当前实现端点候选的本机同范围检查为38通过、5跳过、120.37秒。三项跳过因本机PyTorch无Flash内核，A800另有实际通过证据；两项原真实T0检查需要本机未复制的非测试资产，未冒充已运行。结果为`tmp/density-preexperiments/implementation-end.xml`。
- 实现分支为`codex/density-preexperiments`，共同基点为`a78ae8f36155a4bdae54aa79b6d30f79ffebc59e`；未接受的SMILES候选留在`codex/pre-smiles`，不混入当前正式入口。学习分支为`Learn/density-preexperiments`；精确端点、树一致性及验证结果保存于`tmp/density-preexperiments/dual-endpoints.json`，通过后快进`Learn/CUMULATIVE`。临时验收代码保留历史和磁盘证据，退出最终Git活跃树。

主代理另外独立从原始metrics重算第20、23、24次的预取后三段GPU中位数与等待比例，与逐实验表格一致。最终四配置的布局及中文注释窄复核通过，MADV候选不进入正式实现。

源契约分别为[科学契约](../../想法/方案草稿/9-8-科学契约.md)、[工程细节](../../想法/方案草稿/9-8-工程与实现细节.md)及[边界清单](../../想法/方案草稿/9-8-边界与核查清单.md)。

## 产物、资源与正式运行命令

密度任务证据根为`/storage/penghongen/tmp/pocketxmol_density_20260914`，每次冻结代码、launch、数值检查、性能JSON和失败日志均保留。SMILES证据根为`/storage/penghongen/tmp/pxm_pre_smiles_20260914`；候选坐标位于`/storage/penghongen/AdaLigand/Ori_Data/smiles_assets/SMILE_coords/v1`，候选清单位于`/storage/penghongen/PocketXMol/data/smiles-v1/frozen`，未作为正式数据启用。

获准资源仍为371591与379402_0（实际JobId379402），均位于gnode09，各16CPU。未删除服务器产物、未使用scancel；379402已只读核实after_lock与try_lock存在、kill_lock不存在，控制器PID2383 sleeping且无子进程，指定GPU无计算进程；证据为`tmp/pre-density-20260914/final_resource_status.json`。371591最终只读核实PID20225 sleeping、无子进程，after_lock及父try_lock保留、kill_lock不存在，指定GPU0%／5MiB且无计算进程。两项资源保留，全部GPU尝试在18:34前结束。密度预实验实际使用隔离`deps/`中的einops0.8.1，未修改公共Conda或项目venv；本次后续集成已将einops0.8.1安装到PocketXMol专用venv并核对导入路径，公共Conda仍未改动。

环境准备命令（已执行，非正式运行或验收）：`/storage/penghongen/PocketXMol/runtime/venv/bin/python -m pip install --no-deps einops==0.8.1`。导入来自该venv的`lib/python3.10/site-packages/einops/__init__.py`。

本阶段没有正式训练、测试或评价命令，也没有新正式W&B运行。后续正式模型按D1→D4→D2→D3，每个中心／包络两模型，须先解决SMILES科学输入边界；正式短入口在实际启动前与确定配置、best和产物目录一起记录。

## 测试与只读核查命令

本机端点测试命令：`tmp/pxm-20260910/venv/Scripts/python.exe -X utf8 -B -m pytest tests/test_density.py tests/test_density_dataset.py tests/test_density_sampling.py tests/test_docking_official.py tests/test_docking_centers.py -q --basetemp=tmp/density-preexperiments/pytest-implementation-end --junitxml=tmp/density-preexperiments/implementation-end.xml`。环境为PYTHONPATH指向当前仓库、OMP_NUM_THREADS=1、MKL_NUM_THREADS=1、PYTHONDONTWRITEBYTECODE=1。学习端点使用相同范围和独立输出目录。

各次GPU性能、坐标迁移及数值门控命令在对应实验日志，均为本次预实验命令，不能当作正式运行入口。主代理只读核对第19—24次原始JSON的完成状态、完整更新数、参数有限性、耗时、等待和显存；没有通过读取日志修改服务器状态。


## 实现、验收及失败的过程记录

以下保留准备、调试和窄复核的证据及当时判断，含后续回填；其中“正在”或“待完成”是过程状态，当前结论以前部及对应实验日志为准。

密度56通道模块按用户最新要求保留原代码及全部通道配置。主代理曾作未提交的固定56通道精简，在用户明确要求保持原模块后已撤回；不将短代码视作优化收益。当前原模块与Pocket_Plus源码在5种48³构造输入（稀疏受体、密集受体、空受体、恒定图、零图，含非连续数组切片）上56通道逐元素一致，且没有修改源数组。证据 `tmp/density-preexperiments/channel-source-current.json`；源文件SHA256为 `c34c818105591ba56f4d0a244b12d07ab4e8153dc5862b892fb45dbe488fa982`，仅用于这次验收。

17:24稳定实现状态：批量读出已集成至2a72ce8，数学代码与子代理通过门控的eef5718在忽略Docstring后AST相同，见 `tmp/density-preexperiments/readout-integration-ast.json`。实际medium精度与highest FP32参考的8组D1/D4、距离开关、混合精度组合通过；主代理读取原始JSON核实最大输出绝对差2.77157e-5、最大非零梯度相对RMS差0.00688379。旧bf16结果、理论零梯度和全部活跃参数的比较保留在 `tmp/pre-density-20260914/readout_equivalence_final.json`，不宣称与旧bf16逐元素相同。

完整D4新样本窗口（种子2024、24×3、32个加载进程）完成16更新：热态均值4.69653秒，读取等待0.017825%；代理统计GPU平均97.10%、RSS79.81GB、已分配显存44.36GB。主代理独立读 `/storage/penghongen/tmp/pocketxmol_density_20260914/benchmarks/09_D4_b24_w32_full_batched_fp32/result.json` 确认16更新、均值、等待和最终参数有限。GPU平均值不替代三个稳定窗口中位数验收；各批量组合、距离项成本和最终推荐仍待完成。

SMILES 不支持比例要求为已检查 occurrence 中严格小于1%。全四划分共有66878个实例；若全部完成，最多668个不支持。未完成时同时报告已检查数、失败数、实际比例、覆盖方式和未检查范围；不把样本比例写成全量证明。训练及 val/loss 可跳过明确不支持实例，正式测试保持446个实例及候选评价分母。

性能预实验使用有效全局批量72，优先24×3、36×2、72×1。普通 FlashAttention 为速度参照；平方距离偏置若使完整更新耗时增加超过10%，正式训练前统一退回普通注意力。此比较包含在同一个三小时、24次上限内。目标 GPU 利用率中位数约90%、数据等待约不超过10%；预算内未达目标但科学与稳定性验收通过时，报告最快稳定配置并推进正式阶段。

16:54完整清单状态：D1第6次基准（24×3、15个加载进程、逐分子读出）完成24更新，热态平均4.647秒，读取等待46.22%；代理统计GPU约34.83%、CPU约4.73/16核、RSS41.29GB。主代理独立读取 `/storage/penghongen/tmp/pocketxmol_density_20260914/benchmarks/06_D1_b24_w15_full_reference/result.json` 核实更新数、均值与等待比例。前几次2.4–2.5秒及近零等待由预取掩盖，不能作为稳定结论。下一轮在该作业16CPU配额内尝试32个单线程加载进程以覆盖I/O等待，并核对内存；不会因此申请额外CPU或更换实例抽样分布。

### 资源与文件边界

371591控制目录为 `/home/penghongen/Feedback/Pocket_Plus/allocations/371591`。379402的控制目录为 `/storage/penghongen/Adaligand_infered_receptor_data/cryoatom2/calibration/运行日志与统计/slurm/allocations/379402`；实际锁编号为379402。接管前重新核对作业、进程和实际控制协议，保留 after_lock。未经许可不删除旧资产、不用 scancel 代替锁协议。

两张卡在同一主机，优先使用各自16核CPU；性能窗口应避开另一项预实验的大量I/O。额外CPU分片每任务8核、全团队合计最多96核，排队时优先使用A800自带CPU。

源 SMILES 图及排列来自 `/storage/penghongen/AdaLigand/Ori_Data/smiles_assets/smiles_graphs_v1.npz` 和 `smiles_symmetries_v1.npz`。本次用户明确允许在同根的 `SMILE_coords` 新增重排坐标；其余 AdaLigand 源资产仍保持只读，已有同名文件不得直接覆盖。

### 正式运行命令

尚未启动正式密度训练、测试或评价。正式阶段沿用训练、采样、评价三个短脚本入口，具体配置和best路径在运行前固定并记录。一次性坐标迁移属于下面的迁移／验收命令，不充当正式训练命令。

### 迁移、验收、性能与只读核查命令

本阶段启动前已执行本地只读 Git 检查：`git status --short`、`git worktree list --porcelain`、`git log --all --format='%ct %H'`。确认工作区干净，按提交者时间唯一最新提交为 `a78ae8f36155a4bdae54aa79b6d30f79ffebc59e`。

具体迁移及 GPU 命令由对应实验日志逐次记录，本处不把尚未执行的命令写成通过证据。

主代理本地集成验收命令为 `tmp/pxm-20260910/venv/Scripts/python.exe -X utf8 -B -m pytest tests/test_density_dataset.py tests/test_density.py -q --basetemp=tmp/density-preexperiments/pytest-integration-2 --junitxml=tmp/density-preexperiments/integration-2.xml`。运行环境设置PYTHONPATH为仓库根、OMP_NUM_THREADS=1、MKL_NUM_THREADS=1、PYTHONDONTWRITEBYTECODE=1；10项通过，8.15秒。覆盖C0/C5/E的原点及输入不变、实际裁块拼批、DataModule从model.density派生唯一配置，以及密度模块六项数值／几何检查；它不替代真实GPU验收。

随后完整回归命令为 `tmp/pxm-20260910/venv/Scripts/python.exe -X utf8 -B -m pytest tests -q --basetemp=tmp/density-preexperiments/pytest-regression-1 --junitxml=tmp/density-preexperiments/regression-1.xml`，环境设置同上。结果为53通过、3跳过、3失败，耗时126.55秒。三个失败均为既有tests/test_docking_gpu.py在本机读取服务器路径 `/storage/penghongen/PocketXMol_official_test/extracted/data/trained_models/pxm/checkpoints/pocketxmol.ckpt` 时FileNotFoundError，尚未执行其GPU科学验收；不将其写成通过，也不修改测试跳过规则。新密度CUDA边界检查已通过。完整XML保留失败栈。

主代理对docking/density.py、models/density_blocks.py核对职责、调用与张量语义后补充输入输出Docstring，纠正旧循环特征描述及AttentionGate门控组宽度说明。忽略Docstring后的AST与修改前HEAD一致，证据为 `tmp/density-preexperiments/comment-ast-1.json`，没有改变这些文件的运算。

主代理继续核对密度编码器和原子读出，补全Q/K/V轴、距离偏置精度、源体素与局部坐标、空邻域语义。两模块忽略Docstring后的AST与bd86fd9相同，见 `tmp/density-preexperiments/comment-ast-2.json`。密度模块的导入移入model.density明确分支，避免现有无密度入口因服务器尚未安装einops而失效；在新Python进程中令sys.modules['einops']=None后，原RA模型成功构造且未加载density_backbone。

集成新增注意力检查时，本地torch2.4.1缺少is_flash_attention_available能力查询API，第一次密度集合复测为12通过、1项在能力查询处失败。仅测试改为缺少能力报告时明确跳过普通Flash项目；A800/Linux仍须实际执行。之后相同集合复测为12通过、1跳过，25.86秒，命令为 `tmp/pxm-20260910/venv/Scripts/python.exe -X utf8 -B -m pytest tests/test_density.py tests/test_density_dataset.py -q --basetemp=tmp/density-preexperiments/pytest-integration-4 --junitxml=tmp/density-preexperiments/integration-4.xml`，环境同前。未放宽任何数值门限，A800的Flash输出和梯度另有真实通过记录。

通道源码比较命令为 `tmp/pxm-20260910/venv/Scripts/python.exe -X utf8 -B tmp/density-preexperiments/check_channel_source.py --source C:/Users/15919/Desktop/Pocket_Plus/src/datasets/density_channel_builder.py --output tmp/density-preexperiments/channel-source-current.json`。这是一次性数值验收，不是正式运行命令。

主代理通过统一SSH只读入口独立读取cpu-v1/migration_report.json，确认checked=66878、failed=0、各划分65290/781/361/446和公共图版本一致。随后GPU出现的离散键差异说明初轮sanitize后同构检查不足，补核结果须另列，不把初轮零失败沿用为最终支持率。

### 实现与审查记录

共同基点为 `a78ae8f36155a4bdae54aa79b6d30f79ffebc59e`。主实现分支为 `codex/density-preexperiments`；SMILES与密度子代理分别在 `codex/pre-smiles`、`codex/pre-density` 的独立工作树实现。主代理负责公共入口集成、三契约、共享日志与双线收口。

主代理提交独立审查前进行职责布局、注释与科学步骤两遍自查；独立布局／Git、注释、科学逻辑审查各一轮全面检查，之后只核查已提出问题。预实验验收与双线收口完成后再建立正式八模型goal。

稳定代码交审前的两遍主代理自查已完成。第一遍依次核对docking/density.py的读取与裁块、OccurrenceDataset的构造/装配、DataModule.setup及官方参数装载、密度卷积和VolumeAttention/DensityEncoder、density_attention/DensityReadout、PMAsymDenoiser与ContextNodeEdgeNet的前向、sample_occurrence/sample_docking，以及对应配置和新增测试；修正了可选依赖导入、读出单次包装、候选缓存边界和源几何的精度表达。第二遍按相同文件顺序核对类、函数及行级说明，补齐输入输出形状、ZYX/XYZ、分子归属、FP32几何、残差和首维缓存语义；ALL原模块按用户要求保持原样。新缓存没有修改原sample_loop3、dock噪声、loss或置信度计算。

交审前集成检查命令为 `tmp/pxm-20260910/venv/Scripts/python.exe -X utf8 -B -m pytest tests/test_density.py tests/test_density_dataset.py tests/test_density_sampling.py -q --basetemp=tmp/density-preexperiments/pytest-integrated-5 --junitxml=tmp/density-preexperiments/integrated-5.xml`，环境同前。19通过、1跳过，63.67秒；跳过仍为本机未提供普通Flash能力报告，A800另有实际门控。新增检查包括两分子不同角点/旋转/间距、highest与medium的贴边几何，以及缓存后的真实GPU采样。测试仅使用构造数据，不读取正式测试集。

主代理已逐文件核对数据加载、Dataset、DataModule、模型入口、密度编码器、卷积模块、读出及六层注入点，补充PMAsymDenoiser类级密度输入契约，避免旧“条件只来自口袋”的说明遗漏新增输入。该段为前期自查记录；稳定版本的两遍自查已在上文记录，独立审查尚未收口。

密度骨干和复用卷积模块的构造／前向方法已补齐简短职责Docstring，与类说明的形状契约相互对应。忽略Docstring后的AST与48a067b完全一致，证据 `tmp/density-preexperiments/comment-ast-4.json`；原始56通道模块保持不动。

推理固定编码复用已接入：每个实例定位后编码一次当前48³裁块，通过显式density_feature参数传给原采样循环中的模型调用；候选批量只展开特征首维，训练拒绝缓存，原噪声及置信度不变。本机RTX4060 Laptop、torch2.4.1的真实采样检查覆盖D1/D4、3候选分为2和1两批、每批2步；逐步比较全部预测／置信度、GT坐标清零和固定裁块原点。严格FP32（仅验收上下文关闭cuDNN TF32）两项通过，58.61秒，命令为 `tmp/pxm-20260910/venv/Scripts/python.exe -X utf8 -B -m pytest tests/test_density_sampling.py -q --basetemp=tmp/density-preexperiments/pytest-sampling-2 --junitxml=tmp/density-preexperiments/sampling-2.xml`，环境同前。这不是A800正式吞吐测量，也不是100步最终性能证明；正式精度设置没有改变。

独立科学审查完成一轮全面核查，发现一处P1：实际配置强制Flash，采样缓存编码却是FP32，原VolumeAttention内核入口不能执行。修复限定在瓶颈Flash入口：FP32的Q/K/V转bf16，内核输出恢复原value类型；原训练autocast、卷积、投影和T0采样机制不变。缓存/重新编码测试增加真实flash配置的D1/D4组合，本机不具备Flash的项目明确跳过，由获准A800补验收。修复后只对此问题窄复核，不再扩大科学审查。布局与注释由另一名独立代理合并审查，保留用户授予的布局审核批准职责。

Flash修复后的本机窄验收命令为 `tmp/pxm-20260910/venv/Scripts/python.exe -X utf8 -B -m pytest tests/test_density_sampling.py -q --basetemp=tmp/density-preexperiments/pytest-sampling-3 --junitxml=tmp/density-preexperiments/sampling-3.xml`，环境同前。2项sdpa通过、2项Flash因本机PyTorch未编译内核而跳过，55.99秒；A800实际Flash项目尚待执行，不能由本机结果替代。

布局／注释审查指出的当前快照冲突已统一，补齐D1 valid的真假语义、最大原子数及D4逐原子邻域、四头归约形状。复制的density_blocks.py行尾空白已统一；density_channels.py依用户原样保留，基点到端点的diff-check可能报告该文件原有空白，须作为明确例外记录，不能声称全范围无空白问题。

独立布局／注释窄复核基于ab615f5通过，所提快照、形状说明及blocks空白问题均关闭。布局批准的具名例外包括只读缓存read_density_source、Dataset密度构造入口load_density_input、可独立数值验收的density_attention、nn.Module构造与前向和成熟卷积模块接口；ALL原模块按用户要求保留。科学P1静态窄复核已通过，仍待A800实际Flash运行；最终Git端点核查另行完成。

性能与正式入口对照发现临时基准启用cuDNN benchmark并设prefetch=1，原train_pl没有对应接线。主代理补充可选train.cudnn_benchmark至Trainer、train.prefetch_factor至多进程DataLoader及续训资源排除键；无配置保留原默认，workers=0不传预取参数。最终密度配置须明确两项，避免把临时基准的速度外推给不同正式设置。

主代理独立读取第10—14次原始result.json，核实D1的24×3／36×2／72×1热态均值为1.614201／1.476974／1.366064秒，已分配显存26.634／38.188／66.209GB。D4的36×2完成16次更新，热态均值4.590298秒、已分配显存66.251GB；72×1虽完成首个forward，但反向OOM且完整更新为0。短窗口仍不足单独证明持续供数。

正式加载接线窄验收命令为 `tmp/pxm-20260910/venv/Scripts/python.exe -X utf8 -B -m pytest tests/test_density_dataset.py -q --basetemp=tmp/density-preexperiments/pytest-loader-6 --junitxml=tmp/density-preexperiments/loader-6.xml`，环境同前。5通过，26.85秒，包括workers=0／2下预取参数行为。审查后注释与空白修订的可执行AST与3b17b5c相同，见 `tmp/density-preexperiments/comment-review-ast.json`；最初完整AST比较因blocks Docstring内部尾空白变化不相等，排除Docstring后确认运算未变，不能将完整AST写作相同。

17:55距离项决定：第15—18次均完整完成、参数有限；主代理独立读result.json核实D1在相同种子2024、global72、23个热更新中，普通Flash为1.345325秒、带距离为1.358320秒，增时0.9659%；D4在15个热更新中，普通局部读出为4.676952秒、带距离为4.666072秒，差异-0.2326%。两种结构均未超过10%，按既定判据统一保留distance_bias=true。D4为343局部注意力，不将其称为Flash读出；这不是新增科学试验。对应原始目录为任务根benchmarks/15_D1_b72_distance0、16_D1_b72_distance1、17_D4_b36_distance0、18_D4_b36_distance1。

实际A800推理窄验收已通过：root 0a9c849归档的D1/D4×sdpa/flash四项均实际执行，4通过、0跳过、19.43秒。release为source_2b2c8cee0d7e，launch为density_sampling_gate_0a9c849；主代理和独立科学审查代理分别只读读取 `/storage/penghongen/tmp/pocketxmol_density_20260914/root_sampling_gate_0a9c849.log` 核实。归档只把.sh转LF，Python保持该提交。P1正式关闭；此前CRLF启动失败留在尝试证据，不冒充数值失败。a940482的预取、cuDNN和续训资源接线也分别通过布局／注释与科学窄复核，确认不改变抽样分布、定位、噪声或监督公式。

第19次D1持续供数窗口使用72×1、15个加载进程、新种子2025，完成60更新且参数有限。代理报告热态均值2.889477秒、等待53.36%，后半均值3.328139秒、等待59.28%，GPU平均43.11%，进程树实际读取142.29GB，已分配显存69.71GB。超过预取容量后未达到供数目标，不能以先前1.37秒计算窗口声称正式训练GPU稳定达标。第20次32进程窗口及D4持续窗口仍在运行；最后一次可在原预算内仅尝试进程局部MADV_RANDOM，抑制无关地图区域预读，不清页缓存、不改资产或全局系统设置。到三小时／24次上限即停止扩大改善并报告实测结果。

第20次D1的32加载进程／种子2026也完成60更新且参数有限，热态均值1.965708秒、等待29.2087%，后半2.251414秒、等待38.3831%，GPU平均64.92%，热窗口读取112.059GB、RSS峰值141.33GB。当前选32进程，仍未达到持续供数目标；不同实例前缀和共享页缓存使两次差异不能完全归因于并发数。四份配置已集成资源值：D1=72×1、D4=36×2、32进程、prefetch_factor=1、cudnn_benchmark=true、distance_bias=true。解析内容与代理e1f9468一致并确认global72，证据 `tmp/density-preexperiments/final-config-integration.json`；保留主代理完整配置注释，未改科学参数。

### 之前的尝试与分叉

推理缓存首次本机验收同时运行 `tests/test_density_sampling.py tests/test_docking_sampling.py`，8通过、1失败，44.41秒，证据 `tmp/density-preexperiments/sampling-1.xml`。D1和7项既有采样测试通过；D4的cuDNN默认允许TF32，单份与双份编码批量导致原子分类输出最大绝对差1.2517e-4，超过本次严格FP32的2e-5绝对门限。限定FP32参考后通过，未放宽门限、未更改正式精度；不把这项参考通过写成默认TF32逐元素相同。

密度第2次基准完成12次D1更新，24×3、8个加载进程，热态均值2.772秒、中位数2.428秒，读取等待11.85%，峰值显存22.54GB、进程树RSS21.18GB，GPU采样约53%。该次在144个实例内循环，只代表小集合反复读取的热态计算基线，不能代表完整训练I/O。主代理已要求后续用完整冻结训练清单均匀打乱，固定种子保证各batch组合前72实例一致，不为缓存命中而循环小子集。D4完整U-Net基准及批量读出优化仍在预算内进行，尚未形成最终推荐。

全量原始模型输入检查报告为 `/storage/penghongen/tmp/pxm_pre_smiles_20260914/diagnose-v1/raw_model_graph_report.json`：66878实例、2039个身份配对中，28650实例、1389个配对不等价；分别为训练27983、验证284、校准118、测试265。主要差异为旧模板的单／双键类别1／2与公共SMILES的芳香键类别4。旧输入未使用类别4。`kekule_feasibility.json` 进一步记录：仅从SMILES独立生成单双键表示，仍有16477实例、653个身份配对不能与旧原始键类型图匹配。因此，化学同构通过不足以证明模型输入一致；继续统一公共编码属于科学输入变更，须由用户决定。旧无密度结果保留；未经批准不补实验、不改模型键编码。

`bf16-repeat-v1/report.json` 对非测试实例6d03/18固定原点和同一物理噪声进行真实GPU诊断。FP32旧输入重复梯度相对差约7.4e-7、7.7e-7，新旧约7.7e-7；原生bf16旧输入重复约12.21%、11.19%，新旧约12.90%。诊断脚本暂时把消息求和升到FP32后再转回，旧重复差为0，新旧梯度差约0.603%、预测坐标最大差2.98e-7 Å。此结果定位了原有bf16原子消息求和的非确定性，只代表一个诊断样本，不能当作全体GPU验收通过。诊断替换没有写入正式代码；是否改变正式归约或验收口径待用户决定。

SMILES首个迁移原型仅按公共重原子数组构造RDKit分子，部分[nH]芳香图因没有显式氢计数而失败；代理改为首次按精确SMILES解析并缓存用于SDF/RMSD的Mol，模型仍读取公共数组，修正后全量CPU通过。原型文件保留，新有效坐标写v1，具体证据归对应实验日志。

主代理首次本地pytest的basetemp父目录尚不存在，造成5项临时目录初始化错误、其余5项通过；创建本次tmp父目录后以新basetemp重跑，10项全部通过。没有因此修改科学代码。

主代理自查发现密度模型bf16 autocast可能降低坐标矩阵乘法精度，改变floor得到的home体素。已由密度代理在几何段禁用autocast、固定FP32计算并补CUDA贴边测试；修正进入f06830c，实际CUDA门控结果待回报。SMILES代理报告bf16梯度相对差约0.09—0.19，超过预设0.02，并发现一个calibration实例的原始halfedge_type映射不等；尚在定位，未放宽阈值、未作为1%例外豁免。

非密度历史见[第一类实验总日志](../第一类实验（不加密度信息）/总日志&分析/总日志.md)，不复制或改写到本目录。

## 本次集成验收（非正式运行）

主分支从92d08d6开始，集成SMILES提交5f7bd8e、固定官方解析测试80ca5ca及D2/D3模型提交0f3ab48。主代理补D3 Dataset语言／标签、训练与val辅助日志、推理固定索引缓存；保留既定原OOM裁批和原dock损失。自查记录为本地`tmp/formal-smiles-density/main-self-review.json`。

测试命令：`tmp/pxm-20260910/venv/Scripts/python.exe -X utf8 -B -m pytest tests/test_density_dataset.py tests/test_density_selection.py tests/test_density_sampling.py tests/test_density_training.py tests/test_density.py tests/test_docking_data.py tests/test_docking_smiles.py tests/test_docking_sampling.py tests/test_docking_centers.py tests/test_docking_official.py tests/test_docking_training.py -q --basetemp=tmp/formal-smiles-density/pytest-main-2 --junitxml=tmp/formal-smiles-density/main-2.xml`。环境PYTHONPATH为项目根、OMP/MKL线程各1。首次main-1因临时父目录不存在，夹具未启动；重试main-2为83通过8跳过，158.58秒。跳过包括Windows无Flash内核和未复制的真实非测试资产，不计为通过。两项临时OOM退出检查与最终契约不符，已连同该修改撤销，后续端点检查以最终代码为准。

真实GPU入口门控已于2026-09-14 20:28:30经379402真实try_lock启动，after_lock保留。源码5af144f，证据根`/storage/penghongen/tmp/pxm_formal_smiles_20260914/density-gate`，日志`gate-5af144f.out/.err`。验收命令为`bash ops/run_docking_gpu_checks.sh -k 'real_data and (D1 or D4 or D2 or D3)' --junitxml <证据根>/training-sampling.xml`，每配置仅2优化器更新、1验证批及2候选3步；之后`python -B -m pytest tests/test_density_sampling.py`核对固定编码与重新编码。全局批量72、真实train/validation、W&B关闭。此为必要入口验收，不是正式实验，不延长已经结束的3小时I/O调优循环。

门控首次启动在正式GPU运算前失败：Git归档中的36份shell文件保留CRLF，发布器报`pipefail`选项含回车。初次out/err和run_cmd均保留，未运行任何模型。仅将本次新源副本的.sh/.sbatch转为LF后，于gnode09时间20:30:46按同一try_lock重试；后缀为`gate-5af144f-r2.out/.err`，launch为`density_gate_5af144f_r2`，归档原包及逐文件换行记录保留。未改科学代码、配置、after_lock或旧产物。
