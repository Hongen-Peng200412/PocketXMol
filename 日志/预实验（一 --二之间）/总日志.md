# 密度正式训练前的两项预实验

当前状态：2026-09-14 16:45，SMILES正式切换暂停，等待用户决定模型键编码契约；密度性能预实验独立继续。全量66878实例中，28650个（42.8392%）的公共SMILES模型键类别与旧输入不同；独立Kekulize后仍有16477个（24.6374%）不能匹配旧键类型图，不能按1%例外豁免。原生bf16旧输入重复运行自身也有梯度非确定性，尚未改变正式归约方式或宣布GPU验收通过。密度D1/D4各完成小集合的12次真实更新，下一轮使用完整训练清单均匀抽样；56通道保持完整ALL实现，读出批量化仍在数值门控，尚无最终性能结论。依据[科学契约](../../想法/方案草稿/9-8-科学契约.md)、[工程细节](../../想法/方案草稿/9-8-工程与实现细节.md)和用户本日最新授权执行。非密度实验已完成收口，历史结果保留。

## 当前状态与产物

| 预实验 | 当前状态 | 主要运行预算 | 获准资源 | 产物与证据 |
|---|---|---|---|---|
| SMILES 构图与 GPU 验收 | 全量原始模型键类别不等价42.8392%，暂停正式切换并等待科学决定；诊断已退出，after_lock保留 | 15:38:59—18:38:59；到时按实际检查样本汇总 | 371591，gnode09，A800，16 CPU | 候选坐标 `/storage/penghongen/AdaLigand/Ori_Data/smiles_assets/SMILE_coords/v1`，候选清单 `/storage/penghongen/PocketXMol/data/smiles-v1/frozen`；证据根 `/storage/penghongen/tmp/pxm_pre_smiles_20260914`，尚不作为正式输入 |
| 密度 I/O 与 GPU 性能 | A800注意力输出／梯度门控、GPU密度pytest及D1/D4真实更新基线完成；原32³ U-Net门控后已补48³对照，读出批量化和完整训练清单I/O仍在验收 | 15:40:56—18:40:56；最多24次尝试，每次首次forward后观察不超过5分钟 | 379402_0，实际 JobId 379402，gnode09，A800，16 CPU | 本地验收 `tmp/density-preexperiments/integration-4.xml`，远端任务根 `/storage/penghongen/tmp/pocketxmol_density_20260914` |

两项各维护一个实验日志，详细日志在代理交付时集成。主要工作三小时到期后停止扩大测试并汇总已有证据；实现、验收、性能测量和审查完成状态分别报告。两个实验均不运行正式测试集以调试模型。

密度56通道模块按用户最新要求保留原代码及全部通道配置。主代理曾作未提交的固定56通道精简，在用户明确要求保持原模块后已撤回；不将短代码视作优化收益。当前原模块与Pocket_Plus源码在5种48³构造输入（稀疏受体、密集受体、空受体、恒定图、零图，含非连续数组切片）上56通道逐元素一致，且没有修改源数组。证据 `tmp/density-preexperiments/channel-source-current.json`；源文件SHA256为 `c34c818105591ba56f4d0a244b12d07ab4e8153dc5862b892fb45dbe488fa982`，仅用于这次验收。

17:24稳定实现状态：批量读出已集成至2a72ce8，数学代码与子代理通过门控的eef5718在忽略Docstring后AST相同，见 `tmp/density-preexperiments/readout-integration-ast.json`。实际medium精度与highest FP32参考的8组D1/D4、距离开关、混合精度组合通过；主代理读取原始JSON核实最大输出绝对差2.77157e-5、最大非零梯度相对RMS差0.00688379。旧bf16结果、理论零梯度和全部活跃参数的比较保留在 `tmp/pre-density-20260914/readout_equivalence_final.json`，不宣称与旧bf16逐元素相同。

完整D4新样本窗口（种子2024、24×3、32个加载进程）完成16更新：热态均值4.69653秒，读取等待0.017825%；代理统计GPU平均97.10%、RSS79.81GB、已分配显存44.36GB。主代理独立读 `/storage/penghongen/tmp/pocketxmol_density_20260914/benchmarks/09_D4_b24_w32_full_batched_fp32/result.json` 确认16更新、均值、等待和最终参数有限。GPU平均值不替代三个稳定窗口中位数验收；各批量组合、距离项成本和最终推荐仍待完成。

SMILES 不支持比例要求为已检查 occurrence 中严格小于1%。全四划分共有66878个实例；若全部完成，最多668个不支持。未完成时同时报告已检查数、失败数、实际比例、覆盖方式和未检查范围；不把样本比例写成全量证明。训练及 val/loss 可跳过明确不支持实例，正式测试保持446个实例及候选评价分母。

性能预实验使用有效全局批量72，优先24×3、36×2、72×1。普通 FlashAttention 为速度参照；平方距离偏置若使完整更新耗时增加超过10%，正式训练前统一退回普通注意力。此比较包含在同一个三小时、24次上限内。目标 GPU 利用率中位数约90%、数据等待约不超过10%；预算内未达目标但科学与稳定性验收通过时，报告最快稳定配置并推进正式阶段。

16:54完整清单状态：D1第6次基准（24×3、15个加载进程、逐分子读出）完成24更新，热态平均4.647秒，读取等待46.22%；代理统计GPU约34.83%、CPU约4.73/16核、RSS41.29GB。主代理独立读取 `/storage/penghongen/tmp/pocketxmol_density_20260914/benchmarks/06_D1_b24_w15_full_reference/result.json` 核实更新数、均值与等待比例。前几次2.4–2.5秒及近零等待由预取掩盖，不能作为稳定结论。下一轮在该作业16CPU配额内尝试32个单线程加载进程以覆盖I/O等待，并核对内存；不会因此申请额外CPU或更换实例抽样分布。

## 资源与文件边界

371591控制目录为 `/home/penghongen/Feedback/Pocket_Plus/allocations/371591`。379402的控制目录为 `/storage/penghongen/Adaligand_infered_receptor_data/cryoatom2/calibration/运行日志与统计/slurm/allocations/379402`；实际锁编号为379402。接管前重新核对作业、进程和实际控制协议，保留 after_lock。未经许可不删除旧资产、不用 scancel 代替锁协议。

两张卡在同一主机，优先使用各自16核CPU；性能窗口应避开另一项预实验的大量I/O。额外CPU分片每任务8核、全团队合计最多96核，排队时优先使用A800自带CPU。

源 SMILES 图及排列来自 `/storage/penghongen/AdaLigand/Ori_Data/smiles_assets/smiles_graphs_v1.npz` 和 `smiles_symmetries_v1.npz`。本次用户明确允许在同根的 `SMILE_coords` 新增重排坐标；其余 AdaLigand 源资产仍保持只读，已有同名文件不得直接覆盖。

## 正式运行命令

尚未启动正式密度训练、测试或评价。正式阶段沿用训练、采样、评价三个短脚本入口，具体配置和best路径在运行前固定并记录。一次性坐标迁移属于下面的迁移／验收命令，不充当正式训练命令。

## 迁移、验收、性能与只读核查命令

本阶段启动前已执行本地只读 Git 检查：`git status --short`、`git worktree list --porcelain`、`git log --all --format='%ct %H'`。确认工作区干净，按提交者时间唯一最新提交为 `a78ae8f36155a4bdae54aa79b6d30f79ffebc59e`。

具体迁移及 GPU 命令由对应实验日志逐次记录，本处不把尚未执行的命令写成通过证据。

主代理本地集成验收命令为 `tmp/pxm-20260910/venv/Scripts/python.exe -X utf8 -B -m pytest tests/test_density_dataset.py tests/test_density.py -q --basetemp=tmp/density-preexperiments/pytest-integration-2 --junitxml=tmp/density-preexperiments/integration-2.xml`。运行环境设置PYTHONPATH为仓库根、OMP_NUM_THREADS=1、MKL_NUM_THREADS=1、PYTHONDONTWRITEBYTECODE=1；10项通过，8.15秒。覆盖C0/C5/E的原点及输入不变、实际裁块拼批、DataModule从model.density派生唯一配置，以及密度模块六项数值／几何检查；它不替代真实GPU验收。

随后完整回归命令为 `tmp/pxm-20260910/venv/Scripts/python.exe -X utf8 -B -m pytest tests -q --basetemp=tmp/density-preexperiments/pytest-regression-1 --junitxml=tmp/density-preexperiments/regression-1.xml`，环境设置同上。结果为53通过、3跳过、3失败，耗时126.55秒。三个失败均为既有tests/test_docking_gpu.py在本机读取服务器路径 `/storage/penghongen/PocketXMol_official_test/extracted/data/trained_models/pxm/checkpoints/pocketxmol.ckpt` 时FileNotFoundError，尚未执行其GPU科学验收；不将其写成通过，也不修改测试跳过规则。新密度CUDA边界检查已通过。完整XML保留失败栈。

主代理对docking/density.py、models/density_blocks.py核对职责、调用与张量语义后补充输入输出Docstring，纠正旧循环特征描述及AttentionGate门控组宽度说明。忽略Docstring后的AST与修改前HEAD一致，证据为 `tmp/density-preexperiments/comment-ast-1.json`，没有改变这些文件的运算。

主代理继续核对密度编码器和原子读出，补全Q/K/V轴、距离偏置精度、源体素与局部坐标、空邻域语义。两模块忽略Docstring后的AST与bd86fd9相同，见 `tmp/density-preexperiments/comment-ast-2.json`。密度模块的导入移入model.density明确分支，避免现有无密度入口因服务器尚未安装einops而失效；在新Python进程中令sys.modules['einops']=None后，原RA模型成功构造且未加载density_backbone。

集成新增注意力检查时，本地torch2.4.1缺少is_flash_attention_available能力查询API，第一次密度集合复测为12通过、1项在能力查询处失败。仅测试改为缺少能力报告时明确跳过普通Flash项目；A800/Linux仍须实际执行。之后相同集合复测为12通过、1跳过，25.86秒，命令为 `tmp/pxm-20260910/venv/Scripts/python.exe -X utf8 -B -m pytest tests/test_density.py tests/test_density_dataset.py -q --basetemp=tmp/density-preexperiments/pytest-integration-4 --junitxml=tmp/density-preexperiments/integration-4.xml`，环境同前。未放宽任何数值门限，A800的Flash输出和梯度另有真实通过记录。

通道源码比较命令为 `tmp/pxm-20260910/venv/Scripts/python.exe -X utf8 -B tmp/density-preexperiments/check_channel_source.py --source C:/Users/15919/Desktop/Pocket_Plus/src/datasets/density_channel_builder.py --output tmp/density-preexperiments/channel-source-current.json`。这是一次性数值验收，不是正式运行命令。

主代理通过统一SSH只读入口独立读取cpu-v1/migration_report.json，确认checked=66878、failed=0、各划分65290/781/361/446和公共图版本一致。随后GPU出现的离散键差异说明初轮sanitize后同构检查不足，补核结果须另列，不把初轮零失败沿用为最终支持率。

## 实现与审查记录

共同基点为 `a78ae8f36155a4bdae54aa79b6d30f79ffebc59e`。主实现分支为 `codex/density-preexperiments`；SMILES与密度子代理分别在 `codex/pre-smiles`、`codex/pre-density` 的独立工作树实现。主代理负责公共入口集成、三契约、共享日志与双线收口。

主代理提交独立审查前进行职责布局、注释与科学步骤两遍自查；独立布局／Git、注释、科学逻辑审查各一轮全面检查，之后只核查已提出问题。预实验验收与双线收口完成后再建立正式八模型goal。

稳定代码交审前的两遍主代理自查已完成。第一遍依次核对docking/density.py的读取与裁块、OccurrenceDataset的构造/装配、DataModule.setup及官方参数装载、密度卷积和VolumeAttention/DensityEncoder、density_attention/DensityReadout、PMAsymDenoiser与ContextNodeEdgeNet的前向、sample_occurrence/sample_docking，以及对应配置和新增测试；修正了可选依赖导入、读出单次包装、候选缓存边界和源几何的精度表达。第二遍按相同文件顺序核对类、函数及行级说明，补齐输入输出形状、ZYX/XYZ、分子归属、FP32几何、残差和首维缓存语义；ALL原模块按用户要求保持原样。新缓存没有修改原sample_loop3、dock噪声、loss或置信度计算。

交审前集成检查命令为 `tmp/pxm-20260910/venv/Scripts/python.exe -X utf8 -B -m pytest tests/test_density.py tests/test_density_dataset.py tests/test_density_sampling.py -q --basetemp=tmp/density-preexperiments/pytest-integrated-5 --junitxml=tmp/density-preexperiments/integrated-5.xml`，环境同前。19通过、1跳过，63.67秒；跳过仍为本机未提供普通Flash能力报告，A800另有实际门控。新增检查包括两分子不同角点/旋转/间距、highest与medium的贴边几何，以及缓存后的真实GPU采样。测试仅使用构造数据，不读取正式测试集。

主代理已逐文件核对数据加载、Dataset、DataModule、模型入口、密度编码器、卷积模块、读出及六层注入点，补充PMAsymDenoiser类级密度输入契约，避免旧“条件只来自口袋”的说明遗漏新增输入。密度读出仍有性能修改，最终两遍自查及独立审查须覆盖其稳定版本；目前不宣称审查收口。

密度骨干和复用卷积模块的构造／前向方法已补齐简短职责Docstring，与类说明的形状契约相互对应。忽略Docstring后的AST与48a067b完全一致，证据 `tmp/density-preexperiments/comment-ast-4.json`；原始56通道模块保持不动。

推理固定编码复用已接入：每个实例定位后编码一次当前48³裁块，通过显式density_feature参数传给原采样循环中的模型调用；候选批量只展开特征首维，训练拒绝缓存，原噪声及置信度不变。本机RTX4060 Laptop、torch2.4.1的真实采样检查覆盖D1/D4、3候选分为2和1两批、每批2步；逐步比较全部预测／置信度、GT坐标清零和固定裁块原点。严格FP32（仅验收上下文关闭cuDNN TF32）两项通过，58.61秒，命令为 `tmp/pxm-20260910/venv/Scripts/python.exe -X utf8 -B -m pytest tests/test_density_sampling.py -q --basetemp=tmp/density-preexperiments/pytest-sampling-2 --junitxml=tmp/density-preexperiments/sampling-2.xml`，环境同前。这不是A800正式吞吐测量，也不是100步最终性能证明；正式精度设置没有改变。

## 之前的尝试与分叉

推理缓存首次本机验收同时运行 `tests/test_density_sampling.py tests/test_docking_sampling.py`，8通过、1失败，44.41秒，证据 `tmp/density-preexperiments/sampling-1.xml`。D1和7项既有采样测试通过；D4的cuDNN默认允许TF32，单份与双份编码批量导致原子分类输出最大绝对差1.2517e-4，超过本次严格FP32的2e-5绝对门限。限定FP32参考后通过，未放宽门限、未更改正式精度；不把这项参考通过写成默认TF32逐元素相同。

密度第2次基准完成12次D1更新，24×3、8个加载进程，热态均值2.772秒、中位数2.428秒，读取等待11.85%，峰值显存22.54GB、进程树RSS21.18GB，GPU采样约53%。该次在144个实例内循环，只代表小集合反复读取的热态计算基线，不能代表完整训练I/O。主代理已要求后续用完整冻结训练清单均匀打乱，固定种子保证各batch组合前72实例一致，不为缓存命中而循环小子集。D4完整U-Net基准及批量读出优化仍在预算内进行，尚未形成最终推荐。

全量原始模型输入检查报告为 `/storage/penghongen/tmp/pxm_pre_smiles_20260914/diagnose-v1/raw_model_graph_report.json`：66878实例、2039个身份配对中，28650实例、1389个配对不等价；分别为训练27983、验证284、校准118、测试265。主要差异为旧模板的单／双键类别1／2与公共SMILES的芳香键类别4。旧输入未使用类别4。`kekule_feasibility.json` 进一步记录：仅从SMILES独立生成单双键表示，仍有16477实例、653个身份配对不能与旧原始键类型图匹配。因此，化学同构通过不足以证明模型输入一致；继续统一公共编码属于科学输入变更，须由用户决定。旧无密度结果保留；未经批准不补实验、不改模型键编码。

`bf16-repeat-v1/report.json` 对非测试实例6d03/18固定原点和同一物理噪声进行真实GPU诊断。FP32旧输入重复梯度相对差约7.4e-7、7.7e-7，新旧约7.7e-7；原生bf16旧输入重复约12.21%、11.19%，新旧约12.90%。诊断脚本暂时把消息求和升到FP32后再转回，旧重复差为0，新旧梯度差约0.603%、预测坐标最大差2.98e-7 Å。此结果定位了原有bf16原子消息求和的非确定性，只代表一个诊断样本，不能当作全体GPU验收通过。诊断替换没有写入正式代码；是否改变正式归约或验收口径待用户决定。

SMILES首个迁移原型仅按公共重原子数组构造RDKit分子，部分[nH]芳香图因没有显式氢计数而失败；代理改为首次按精确SMILES解析并缓存用于SDF/RMSD的Mol，模型仍读取公共数组，修正后全量CPU通过。原型文件保留，新有效坐标写v1，具体证据归对应实验日志。

主代理首次本地pytest的basetemp父目录尚不存在，造成5项临时目录初始化错误、其余5项通过；创建本次tmp父目录后以新basetemp重跑，10项全部通过。没有因此修改科学代码。

主代理自查发现密度模型bf16 autocast可能降低坐标矩阵乘法精度，改变floor得到的home体素。已由密度代理在几何段禁用autocast、固定FP32计算并补CUDA贴边测试；修正进入f06830c，实际CUDA门控结果待回报。SMILES代理报告bf16梯度相对差约0.09—0.19，超过预设0.02，并发现一个calibration实例的原始halfedge_type映射不等；尚在定位，未放宽阈值、未作为1%例外豁免。

非密度历史见[第一类实验总日志](../第一类实验（不加密度信息）/总日志&分析/总日志.md)，不复制或改写到本目录。
