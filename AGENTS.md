# PocketXMol Agent 入口

本文件保存项目级触发入口、关键位置和少量必须始终生效的项目规则。其余具体执行规则只在对应 skill 中维护。

## 规划、执行记录与收口

涉及规划文档、执行记录、映射索引、计划与实现差异、计划回填或收口时，使用 `plan-log-governance`。

关键位置：

- 规划文档：`想法/方案草稿/9-8-科学契约.md`、`9-8-工程与实现细节.md`、`9-8-边界与核查清单.md`。
- 总工作日志：`日志/总日志.md`；逐实验记录：`日志/第一类实验（不加密度信息）/`。训练、推理、共同数据准备分别留有可追溯记录。
- 计划与实现映射：`日志/计划执行映射.md`。

## 项目内容修改

- 涉及代码编写、修改、重构、调试、审查、解释或学习注释时，使用 `ai-code-workflow`，由它调用本次任务需要的具体 skills。本项目对 git 使用双线历史管理策略，同时采用 `dual-track-git-workflow` 管理实现历史与学习历史，并在项目入口重复以下强制规则：
    - 开始实现前，Learn/CUMULATIVE 必须指向本仓库按提交者时间形成的唯一最新提交，且未提交改动都必须在 Learn/CUMULATIVE 上；否则先只读查明较新提交、现有分支、工作树和未提交修改之间的关系，并把整理方案交给用户，等到用户再次确认后才能行动。
    - 正式实现从该 Learn/CUMULATIVE 提交开始，在实现分支保存真实工作过程。实现稳定后，按人类理解顺序建立或补全学习分支；只有实现端点与学习端点通过等价核验后，才能将 Learn/CUMULATIVE 快进到学习端点。任务结束时，Learn/CUMULATIVE 必须再次指向本仓库按提交者时间形成的唯一最新提交。  
    - 当仅仅进行规划（包括落盘.md形式的讨论或计划文档）、进行明显小型的修改或用户明确要求时，可以把改动留在 Learn/CUMULATIVE 的工作区，由用户处理；AI 不得擅自提交、丢弃或混入开始任务前已经存在的未提交修改。

## 面向用户的文字

编写或修改任何面向用户的技术文字时，使用 `technical-expression-workflow`。

任何面向用户的中文或英文文字——包括聊天、代码注释、Docstring、README、讨论文档、规划文档和执行记录——都必须在不依赖未写出的对话、运行历史或作者心中隐含信息时可以理解。优先点名具体文件、数组、字段、函数、实体和处理步骤；无法避免的术语必须在首次出现时解释。特别少用甚至不用 `row（行）`、`proposal`、`route`、`upstream/downstream` 等含义不明确的词；保留必要的代码符号、路径和命令，但不得以英文或中英混杂代替准确解释。
