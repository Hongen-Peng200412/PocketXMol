# PocketXMol Agent 入口

本文件保存项目级触发入口、关键位置和少量必须始终生效的项目规则。其余具体执行规则只在对应 skill 中维护。

## 规划、执行记录与收口

涉及规划文档、执行记录、映射索引、计划与实现差异、计划回填或收口时，使用 `plan-log-governance`。

关键位置：

- 规划文档：`想法/方案草稿/9-8-科学契约.md`、`9-8-工程与实现细节.md`、`9-8-边界与核查清单.md`。
- 全项目阶段索引：`日志/总日志.md`，只保存简短阶段状态与入口；共同准备记录：`日志/实现与共同数据准备.md`。
- 无密度详细总日志：`日志/第一类实验（不加密度信息）/总日志&分析/总日志.md`；同目录保存简要结果分析、RA＋T0决策及收口证据。
- 历史无密度实验目录 `日志/第一类实验（不加密度信息）/` 固定为七份日志：`1-B-C-T0-RA.md`、`2-B-C-T1-RA.md`、`3-B-E-T0-RA.md`、`4-B-C-T0-RB.md`、`5-B-C-T1-RB.md`、`6-B-E-T0-RB.md`、`7-official.md`。每份贯穿该实验的训练、测试与评价；重训、失败、中断、换卡及并行尝试归入对应文件末尾，不按阶段或作业编号另建实验日志。
- 逐实验日志前部优先放当前有效状态、产物路径、检查点、W&B及结果；后部保存执行证据和失败历史。检查取得新进度时同步更新该实验和所属阶段总日志的状态快照，根索引只更新阶段状态；明确阶段完成或分叉时再写handoff，不能用handoff替代实验日志。正式运行命令与测试、门控、只读核查命令分开记录。
- 计划与实现映射：`日志/计划执行映射.md`。
- 两项密度前预实验共享`日志/预实验（一 --二之间）/总日志.md`，每项另有一个贯穿所有尝试的日志；各限三小时，SMILES不支持比例按实际检查实例小于1%。第一次密度实验使用`日志/第二类实验（密度分支的训练）/`，每个D_i一个日志，内部记录中心、包络两个模型。
- `local_cov` 只维护`日志/第三次正式实验/local_cov/本轮实验日志.md`、`C.md`和`E.md`。前者是本轮共享总日志，后两者分别贯穿中心与包络模型的全部尝试。

## 项目内容修改

- 后续科学构造固定RA＋T0，保留原dock噪声、loss、置信度与self-ranking。2026-09-17当前任务是按`想法/方案草稿/9-15.md`实现并完成`local_cov`中心、包络两个模型；外围密度裁块为80³，每个配体原子在六个去噪块中分别读取11³局部窗口。D1、D2属于第一次密度实验，不作为`local_cov`实现基础；D3、D4不启动。
- `local_cov`分支删除且不再引用第一次密度实验专属的`models/density_selection.py`、`models/density_readout.py`、旧`models/density_backbone.py`、`models/density_blocks.py`及其专属配置、测试和入口。通用56维`ALL`密度通道、密度I/O、坐标几何和Dataset公共字段继续复用；新增密度逻辑统一放在`models/density/`。
- 378693／gnode10负责`local_cov`中心模型及C0/C5测试评价；379402（Slurm显示`379402_0`）／gnode09负责包络模型及E测试评价。两个模型使用同一份不可变正式源码release。379403（Slurm显示`379403_1`）只继续D2包络的剩余训练、best E测试和CPU评价；371591不安排任务。所有资源沿真实锁协议保留`after_lock`与历史产物。
- `local_cov`正式重启使用单卡72×1、配置全局批量72；局部卷积分块首选4096，显存不足时只降到2048，之后仍显存不足才改24×3。训练使用bf16-mixed，正式推理沿官方FP32张量路径。优化器、验证、调度、停止、候选预算及W&B规则以`9-15.md`为准。
- 主代理在提交独立审查前完成两遍自查：第一遍检查新增或修改函数的职责、位置、调用关系、嵌套与Docstring；第二遍按`code-comment-style-cn`统一类、函数、数据形状、变量语义和关键科学步骤。随后只进行一轮独立全面审查，对发现的问题仅做窄口径复核。
- 涉及代码编写、修改、重构、调试、审查、解释或学习注释时，使用 `ai-code-workflow`，由它调用本次任务需要的具体 skills。本项目对 git 使用双线历史管理策略，同时采用 `dual-track-git-workflow` 管理实现历史与学习历史，并在项目入口重复以下强制规则：
    - 本次`local_cov`得到一次明确例外：以`d3c1ed548bff97dfe61ab4c052d0c53c47029f2f`为共同基点，`codex/formal-density-strict-ablation`只保存第一次密度实验的真实实现与D1/D2收口；`codex/local-cov`从共同基点独立实现。稳定后从同一基点重建`Learn/local-cov`，端点等价后推进`Learn/CUMULATIVE`。D2交接、最终结果和日志通过独立纯文档提交同步，不把第一次密度实验旧实现带入`local_cov`。
    - 当前仓库内切换formal、local-cov或学习分支前，必须形成工作树干净且范围明确的提交；不建立额外worktree，不用stash保存跨分支改动。
    - 开始实现前，Learn/CUMULATIVE 必须指向本仓库按提交者时间形成的唯一最新提交，且未提交改动都必须在 Learn/CUMULATIVE 上；否则先只读查明较新提交、现有分支、工作树和未提交修改之间的关系，并把整理方案交给用户，等到用户再次确认后才能行动。
    - 正式实现从该 Learn/CUMULATIVE 提交开始，在实现分支保存真实工作过程。实现稳定后，按人类理解顺序建立或补全学习分支；只有实现端点与学习端点通过等价核验后，才能将 Learn/CUMULATIVE 快进到学习端点。任务结束时，Learn/CUMULATIVE 必须再次指向本仓库按提交者时间形成的唯一最新提交。  
    - 当仅仅进行规划（包括落盘.md形式的讨论或计划文档）、进行明显小型的修改或用户明确要求时，可以把改动留在 Learn/CUMULATIVE 的工作区，由用户处理；AI 不得擅自提交、丢弃或混入开始任务前已经存在的未提交修改。

## 面向用户的文字

编写或修改任何面向用户的技术文字时，使用 `technical-expression-workflow`。

任何面向用户的中文或英文文字——包括聊天、代码注释、Docstring、README、讨论文档、规划文档和执行记录——都必须在不依赖未写出的对话、运行历史或作者心中隐含信息时可以理解。优先点名具体文件、数组、字段、函数、实体和处理步骤；无法避免的术语必须在首次出现时解释。特别少用甚至不用 `row（行）`、`proposal`、`route`、`upstream/downstream` 等含义不明确的词；保留必要的代码符号、路径和命令，但不得以英文或中英混杂代替准确解释。
